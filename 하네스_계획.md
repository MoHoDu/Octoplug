현재 이 Unity 프로젝트를 **개인~6인 이하 소규모 팀이 AI와 함께 지속적으로 개발할 수 있는 최종 Harness 구조**로 마이그레이션해줘.

이 작업은 새 프로젝트를 만드는 것이 아니라, **이미 작업 중인 Unity 프로젝트의 기존 문서, 설정, Skill, MCP, 코드 구조를 보존하면서 AI-Native 개발 Harness로 재구성하는 Migration 작업**이다.

중요:
현재 실행 환경은 Claude Code CLI 기반이며, 모델은 Open Codex 등을 통해 변경될 수 있다.  
따라서 Harness는 특정 모델에 종속되지 않게 구성하되, Claude Code CLI에서 즉시 사용할 수 있어야 한다.

이번 작업에서는 게임 기능 자체를 구현하지 않는다.

---

# 0. 최상위 원칙

반드시 다음 원칙을 따른다.

1. Harness가 개발 시스템의 중심이다.
2. Claude Code, Codex, Antigravity, Local Model, Orca는 Harness를 사용하는 도구일 뿐이다.
3. 기존 문서나 설정을 임의로 삭제하지 않는다.
4. 기존 문서의 정보를 잃지 않는다.
5. 기존 구조와 새 구조가 겹치면 무조건 새 파일을 만들지 말고 분석 후 통합한다.
6. 기존 파일을 옮길 때 참조 경로까지 함께 수정한다.
7. 이번 Migration에서는 기존 파일 삭제를 금지한다.
8. 삭제가 필요해 보이는 파일은 최종 보고의 `Delete Candidates`에만 기록한다.
9. 기존 Unity 코드, Scene, Prefab, ScriptableObject, ProjectSettings는 Harness 구축에 꼭 필요하지 않으면 수정하지 않는다.
10. 실제 API Key, Token, Password는 Git 추적 파일에 절대 기록하지 않는다.
11. 문서가 많아져도 Agent가 매번 전부 읽지 않도록 Context Budget 구조를 반드시 적용한다.
12. 불필요한 자동화는 만들지 않는다. 현재 실제로 필요한 최소 구조부터 구축한다.

---

# 1. 먼저 읽기 전용 조사

어떤 파일도 수정하기 전에 현재 저장소를 조사한다.

특히 다음을 확인한다.

- AGENTS.md
- CLAUDE.md
- GEMINI.md 또는 기타 Agent Instruction 파일
- .claude/**
- .codex/**
- .agents/**
- 기존 skills
- 기존 commands
- rules
- MCP 설정
- .mcp.json
- .env
- .env.example
- AI 관련 script
- Unity build/test script
- docs/**
- README
- architecture 문서
- 기획 문서
- 게임 시스템 문서
- coding convention
- Git / branch 관련 문서
- 기존 task / todo / changelog 구조
- 기존 .gitignore
- Unity Assets 폴더 구조
- Tests 구조
- Scene / Prefab 구조의 대략적인 구성

전체 repository를 반복적으로 무작정 탐색하지 않는다.

첫 구조 파악 이후에는 탐색 범위를 줄인다.

---

# 2. Migration Map 먼저 작성

실제 수정 전에 기존 구조 → 목표 Harness 구조의 Migration Map을 작성한다.

예:

| Existing | Target | Action | Reason |
|---|---|---|---|
| docs/player-system.md | docs/domains/player.md | Move + refine | Player Domain Map |
| coding-rules.md | docs/standards/code-conventions.md | Move | Coding standard |
| CLAUDE.md | CLAUDE.md | Keep + reduce | Claude adapter |
| old skill | .agents/skills/... | Merge | Shared skill |

Action은 다음 중 하나로 명시한다.

- Keep
- Move
- Rename
- Merge
- Refine
- Create
- Delete Candidate

Migration Map을 만든 후 그 계획에 따라 작업한다.

---

# 3. 목표 최종 구조

현재 프로젝트에 필요한 최소 구조부터 아래 방향으로 정리한다.

```text
UnityProject/
│
├─ AGENTS.md
├─ CLAUDE.md
│
├─ docs/
│  ├─ architecture.md
│  ├─ domains/
│  │  ├─ INDEX.md
│  │  └─ <domain>.md
│  │
│  ├─ decisions/
│  │  └─ DEC-*.md
│  │
│  └─ standards/
│     └─ code-conventions.md
│
├─ tasks/
│  ├─ TEMPLATE/
│  │  ├─ meta.md
│  │  ├─ plan.md
│  │  ├─ todo.md
│  │  ├─ handoff.md
│  │  └─ log.md
│  │
│  ├─ active/
│  └─ done/
│
├─ harness/
│  ├─ policies/
│  │  ├─ human-decision-gate.md
│  │  ├─ workspace-isolation.md
│  │  ├─ asset-ownership.md
│  │  ├─ git-workflow.md
│  │  ├─ task-lifecycle.md
│  │  └─ context-budget.md
│  │
│  ├─ roles/
│  │  ├─ planner.md
│  │  ├─ developer.md
│  │  ├─ unity-operator.md
│  │  ├─ qa.md
│  │  ├─ art.md
│  │  └─ audio.md
│  │
│  └─ mcp/
│     └─ README.md
│
├─ .agents/
│  └─ skills/
│     ├─ create-task/
│     ├─ plan-task/
│     ├─ request-human-decision/
│     ├─ implement-code/
│     ├─ investigate-bug/
│     ├─ integrate-unity-editor/
│     ├─ verify-unity/
│     ├─ qa-feature/
│     ├─ art-handoff/
│     ├─ audio-handoff/
│     └─ update-domain-map/
│
├─ .claude/
│  ├─ settings.json
│  ├─ settings.local.json
│  └─ skills
│
├─ .codex/
│  └─ config.toml
│
├─ .mcp.json
│
├─ scripts/
│  ├─ setup-links.ps1
│  ├─ create-task.ps1
│  ├─ create-ai-worktree.ps1
│  ├─ remove-ai-worktree.ps1
│  ├─ verify-env.ps1
│  ├─ verify-fast.ps1
│  ├─ verify-unity.ps1
│  ├─ verify-harness.ps1
│  └─ verify-context-budget.ps1
│
├─ .env
├─ .env.example
└─ .gitignore
```

중요:
현재 프로젝트에 필요하지 않은 빈 구조를 형식적으로 전부 만들 필요는 없다.

---

# 4. AGENTS.md는 짧은 지도만

AGENTS.md는 백과사전처럼 만들지 않는다.

역할:

- 프로젝트 짧은 설명
- Domain Index 위치
- 작업 시작 순서
- 핵심 Policy 위치
- Skill 위치
- Task 위치
- 테스트 진입점
- 비밀정보 규칙

상세 Policy나 기획 내용을 AGENTS.md에 복사하지 않는다.

목표:

가능하면 약 100줄 내외의 짧은 안내서로 유지한다.

Agent 기본 시작 순서는 최소 다음을 포함한다.

```text
1. 현재 Task 확인
2. Open Decisions 확인
3. Exclusive Assets 확인
4. 관련 Domain 확인
5. 현재 작업 단계에 필요한 Skill만 읽기
6. 필요한 파일만 탐색
7. 구현/검증
```

---

# 5. CLAUDE.md는 얇은 Adapter

현재 Claude Code CLI가 실제 실행 환경이므로 CLAUDE.md는 유지한다.

하지만 프로젝트 전체 지식을 CLAUDE.md에 중복 작성하지 않는다.

가능하면:

```md
# Claude Project Instructions

@AGENTS.md
```

형태를 기반으로 한다.

기존 CLAUDE.md에 Claude Code 실행에 필요한 중요한 설정이 있다면 제거하지 않는다.

구분:

```text
공통 프로젝트 지식 → AGENTS.md / docs/
Claude 전용 설정 → CLAUDE.md / .claude/
```

---

# 6. Domain Map

기존 문서와 Unity 구조를 바탕으로 실제 프로젝트 Domain을 식별한다.

예:
- Player
- Combat
- Enemy
- UI
- Save
- Stage
- Economy

예시를 그대로 쓰지 말고 실제 프로젝트에 맞게 판단한다.

각 Domain 문서는 **모든 파일 목록이 아니라 핵심 지도**여야 한다.

최소 항목:

```md
# Domain Name

## Role

## Core Files
| Role | Path |
|---|---|

## Related Domains

## Important References
상세 기획서/결정 문서 링크

## Modification Notes

## Search Scope
정보 부족 시 먼저 검색할 폴더
```

원칙:

```text
Domain Map
→ 핵심 파일
→ 좁은 검색
→ 전체 repository 검색은 마지막
```

기존 상세 기획 문서는 삭제하지 않는다.

Domain 문서는 상세 기획의 대체물이 아니라 색인 역할이다.

---

# 7. Human Decision Gate

다음 정책을 추가/통합한다.

```text
harness/policies/human-decision-gate.md
```

AI는 미결정된 기획을 임의로 확정하면 안 된다.

사람이 반드시 결정:

- 게임 규칙
- 밸런스
- 수치
- UX
- UI 행동
- 화면 흐름
- Scene / Prefab 구조 변경
- Art 방향
- Audio 방향
- 보상
- 난이도
- 콘텐츠
- 플레이어 경험에 영향을 주는 결정
- 기존 기획 간 충돌

AI가 스스로 결정 가능:

- 내부 함수명
- 자료구조
- null 처리
- 캐싱
- 테스트 구현
- 내부 코드 구조
- 동일한 결과를 내는 기술 선택

기준:

```text
플레이어 경험 / 기획 변경 → 사람
결정된 경험의 기술 구현 → AI
```

미결정 사항 발견 시 AI는:

1. 결정되지 않은 내용
2. 구현에 필요한 이유
3. 가능한 선택지
4. 선택지별 영향

까지만 제안하고 해당 부분 구현을 중지한다.

---

# 8. Decision 기록

작은 결정:

```text
현재 Task의 Human Decisions
```

장기적으로 유지해야 하는 결정:

```text
docs/decisions/
```

예:

```text
DEC-001-save-policy.md
DEC-002-damage-invincibility.md
```

이미 ADR 구조가 있다면 재사용한다.

같은 질문을 이후 Agent가 반복하지 않게 한다.

---

# 9. Role / Skill 분리

Role은 "누구인가".

Skill은 "어떤 작업을 어떻게 하는가".

## Role

권장:

```text
harness/roles/
planner.md
developer.md
unity-operator.md
qa.md
art.md
audio.md
```

각 Role에는 간단히:

- 책임
- 허용
- 금지
- 사용하는 Skill
- Human Decision Gate 관계

를 기록한다.

## Skill

공통 Skill 원본:

```text
.agents/skills/
```

Skill은 행동 단위로 구성한다.

필요 후보:

```text
create-task
plan-task
request-human-decision
implement-code
investigate-bug
integrate-unity-editor
verify-unity
qa-feature
art-handoff
audio-handoff
update-domain-map
```

기존 Skill과 중복되면 통합한다.

Skill 본문은 짧게 유지한다.

긴 정보는:

```text
references/
scripts/
examples/
```

등에 두고 필요할 때만 읽는다.

---

# 10. Code와 Unity Editor Integration 분리

Harness에서 반드시 두 단계를 분리한다.

## Code Implementation

담당:

- C# 코드
- 게임 로직
- 데이터 로직
- 테스트
- 내부 기술 구현

Skill:

```text
implement-code
```

## Unity Editor Integration

담당:

- Script 연결
- Component 추가
- Inspector Reference
- ScriptableObject 연결
- 반복 Inspector 설정
- Play / Unity 검증

Skill:

```text
integrate-unity-editor
```

작은 Task:

```text
Main Agent
→ implement-code
→ integrate-unity-editor
→ verify-unity
```

같은 Agent가 순차 수행 가능.

큰/위험 Task:

```text
Code Agent
→ Editor Agent
→ QA Agent
```

분리 가능.

즉 "Agent 분리"가 아니라 "작업 단계 분리"가 기본이다.

---

# 11. Unity Editor Integration 규칙

사람이 기본적으로 다음을 준비한다고 가정한다.

- UI
- Scene Hierarchy
- GameObject 구조
- Prefab 기본 구조
- Graphic
- Unity Folder 구조
- 기본 Inspector 값

AI는 기존 구조를 최대한 유지한다.

기본 금지:

- UI 위치/크기 임의 변경
- Graphic 임의 교체
- GameObject Rename
- Scene Hierarchy 대규모 재구성
- Prefab 구조 임의 변경
- Unity Folder 구조 임의 변경
- Asset 위치 임의 이동

허용:

- Script 연결
- Component 추가
- Reference 연결
- 이미 결정된 Inspector 값 입력
- 반복적 설정
- 테스트용 설정

새 GameObject / Prefab / UI / Folder가 필요하면:

1. 기존 구조로 해결 가능한지 확인
2. 기획적 선택인지 확인
3. 필요하면 Human Decision Gate 발동

---

# 12. Human / AI Workspace 분리

다음 Policy를 구성한다.

```text
harness/policies/workspace-isolation.md
```

원칙:

사람과 AI는 동시에 같은 Unity Project Directory를 수정하지 않는다.

## Human Workspace

사람의 Unity Editor 전용.

담당:

- Scene 제작
- UI 제작
- Prefab 제작
- Art 적용
- 다음 Task 준비
- 기획 확인

AI가 Human Workspace의 Unity Editor를 조작하지 않는다.

## AI Workspace

Task별 Git Worktree.

담당:

- Code
- Test
- Unity Editor Integration
- Unity 자동 검증

AI가 Unity를 실행할 때 반드시 자신의 AI Worktree project path를 사용한다.

---

# 13. Asset Ownership

다음 Policy 구성:

```text
harness/policies/asset-ownership.md
```

Task의 `Exclusive Assets`에 적힌 파일은 해당 Task 동안 다른 사람/Agent가 동시에 수정하지 않는다.

특히:

- .unity
- .prefab
- 중요한 ScriptableObject
- ProjectSettings
- Shared UI Prefab

작업 시작 전 Exclusive Assets를 확인한다.

사람은 AI 작업 중 다른 Scene / Prefab / 기능을 준비한다.

---

# 14. Git / Branch / Worktree 규칙

다음 문서 구성:

```text
harness/policies/git-workflow.md
```

현재 프로젝트 기존 Git 규칙이 있다면 우선 재사용한다.

없다면 최소 다음 규칙을 사용한다.

Task ID:

```text
TASK-YYYYMMDD-NNN
```

예:

```text
TASK-20260917-001
```

Task folder:

```text
TASK-20260917-001-shop-purchase
```

Branch:

```text
task/TASK-20260917-001-shop-purchase
```

AI Worktree:

```text
$WORKTREE_ROOT/TASK-20260917-001-shop-purchase
```

절대 경로는 Git 문서에 저장하지 않는다.

개인 `.env`:

```text
WORKTREE_ROOT=D:/UnityWorktrees
```

등으로 관리한다.

Worktree 사용 기준:

```text
단일 작업 + 사람과 AI 동시 작업 없음
→ 일반 branch 가능

사람과 AI 동시 작업
→ AI Worktree

여러 독립 AI 작업
→ Task별 Worktree
```

같은 Scene / Prefab을 두 Worktree에서 동시에 수정하지 않는다.

---

# 15. Code Convention

다음 문서로 기존 Coding Rule을 정리한다.

```text
docs/standards/code-conventions.md
```

기존 프로젝트 Convention을 우선한다.

포함 후보:

- C# Naming
- File/Class 관계
- Unity lifecycle 사용 규칙
- Serialized field 규칙
- ScriptableObject
- Events
- Dependency
- Comment
- Test
- Folder / Namespace

AGENTS.md에는 세부 Convention을 복사하지 말고 해당 문서 링크만 둔다.

---

# 16. Task 구조

모든 개발 작업은 고유 Task ID로 관리할 수 있게 한다.

Task folder:

```text
tasks/active/TASK-20260917-001-shop-purchase/
```

내부:

```text
meta.md
plan.md
todo.md
handoff.md
log.md
```

## meta.md

매우 짧게.

예:

```md
- ID: TASK-20260917-001
- Title: Shop Purchase
- Status: IN_PROGRESS
- Owner: @git-account
- Agent: Codex
- Branch: task/TASK-20260917-001-shop-purchase
- Domain: Shop
- Base: develop
- Started: 2026-09-17
- Exclusive:
  - Assets/Scenes/Shop.unity
```

## plan.md

작업 핵심 단계만 bullet로 작성.

## todo.md

체크리스트.

## handoff.md

다른 Agent / PC / 팀원이 즉시 이어받기 위한 **현재 상태 요약**.

최대 약 20~30줄을 권장한다.

포함:

- Status
- Owner
- Last Agent
- Done
- Pending
- Changed
- Blocked
- Next

## log.md

작업 이력.

bullet 위주.

기록:

- 중요한 변경
- 검증 결과
- 실패 원인
- Human Decision
- Blocker
- Handoff에 필요한 사실

기록하지 않음:

- 단순 파일 열람
- grep 실행
- 사소한 생각 과정
- 대화 전체
- 불필요한 Agent reasoning

log.md는 길어져도 되지만 **기본 Context로 자동 읽지 않는다.**

---

# 17. Task Lifecycle

다음 Policy 구성:

```text
harness/policies/task-lifecycle.md
```

기본 흐름:

```text
Task 생성
↓
Human Decisions / Open Decisions
↓
Scope / Exclusive Assets
↓
Plan
↓
AI Worktree 필요 여부 판단
↓
구현
↓
Editor Integration
↓
Verification
↓
Human Review
↓
Done
```

Agent가 작업을 종료하거나 넘길 때:

```text
todo.md 갱신
handoff.md 갱신
log.md 핵심 항목 추가
```

를 강제한다.

완료 후:

```text
tasks/active/
→ tasks/done/
```

으로 이동한다.

---

# 18. Context Budget

다음 Policy 구성:

```text
harness/policies/context-budget.md
```

가장 중요한 원칙:

```text
저장하는 정보는 많아도 된다.
기본적으로 읽는 정보는 작아야 한다.
```

Agent 시작 시 기본적으로 읽는 것은:

```text
AGENTS.md
현재 Task:
  meta.md
  plan.md
  todo.md
  handoff.md

관련 Domain 1개
현재 단계 Skill 1개
```

기본적으로 읽지 않는 것:

```text
tasks 전체
과거 Task 전체
현재 log.md 전체
모든 Domain
모든 Skill
모든 Policy
모든 Decision
```

추가 정보는 필요할 때만 조회한다.

규칙:

- 연결된 문서를 재귀적으로 전부 읽지 않는다.
- 정보가 충분하면 탐색을 중단한다.
- Domain → 관련 폴더 → repo 전체 검색 순으로 확장한다.
- 과거 log는 필요한 날짜/키워드만 검색한다.
- 과거 Task는 Related Task ID가 있을 때 우선 handoff 또는 요약만 읽는다.

---

# 19. Context Budget 검증

가능하면 AI 호출 없이 실행되는 다음 Script를 만든다.

```text
scripts/verify-context-budget.ps1
```

검사:

- AGENTS.md 길이
- Domain 문서 길이
- Skill 길이
- plan.md 길이
- handoff.md 길이
- 현재 Task 기본 Context 총량

초기 권장 경고선:

```text
AGENTS.md       <= 120 lines
Domain Map      <= 150 lines
SKILL.md        <= 120 lines
plan.md         <= 80 lines
handoff.md      <= 30 lines
```

이 값은 hard failure가 아니라 기본 warning threshold로 구현해도 된다.

`verify-context-budget TASK-ID` 실행 시 가능하면:

```text
AGENTS
Task meta
Task plan
Task todo
Task handoff
Related Domain
Current Skill
```

의 줄 수 / 문자 수를 합산하여 출력한다.

모델별 정확한 tokenizer가 없으면 문자/줄 기준으로 충분하다.

---

# 20. Harness 자체 검증

다음 Script 구성:

```text
scripts/verify-harness.ps1
```

검사:

- 필수 Harness 파일 존재
- Task 파일 구조
- Domain 문서의 Core File 경로 존재 여부
- `.env` ignore 여부
- `.env.example` 존재
- Secret 노출 여부의 기본적인 패턴 검사
- symlink 상태
- Context Budget
- Task handoff stale 여부
- AGENTS → Domain / Policy 링크 유효성

기존 CI 구조가 있다면 연결 가능하되,
초기부터 복잡한 CI를 새로 만들 필요는 없다.

---

# 21. Skill 공유

팀 공용 Skill Single Source of Truth:

```text
.agents/skills/
```

Claude Code:

```text
.claude/skills
→ ../.agents/skills
```

symlink 사용.

기존 `.claude/skills`가 실제 디렉터리라면 임의 삭제하지 않는다.

먼저 내용을 분석하고 `.agents/skills`로 안전하게 통합한 후 symlink를 준비한다.

Windows용:

```text
scripts/setup-links.ps1
```

을 만든다.

---

# 22. .env

프로젝트 루트:

```text
.env
.env.example
```

`.env.example`:

- 변수 이름
- 필요하면 짧은 설명
- 실제 Secret 없음

`.env`:

- 개인 Secret
- WORKTREE_ROOT 등 개인 경로
- Git ignore

기존 변수 이름을 불필요하게 변경하지 않는다.

`.env`가 존재하면 내용 자체를 다른 문서에 출력하지 않는다.

---

# 23. Git 추적 정책

Git 추적:

```text
AGENTS.md
CLAUDE.md
docs/**
tasks/TEMPLATE/**
필요한 task 기록
harness/**
.agents/skills/**
.claude/settings.json
.codex/config.toml
팀 공용 MCP 설정
.env.example
scripts/**
```

Git 제외:

```text
.env
.claude/settings.local.json
.claude/skills
Harness cache
log cache
개인 Agent 설정
개인 MCP
임시 파일
```

개인 Skill / 개인 MCP는 가능하면 프로젝트 내부 ignore 폴더가 아니라 각 도구의 user/global 영역을 사용한다.

---

# 24. MCP

기존 MCP를 우선 보존한다.

다음만 정리한다.

```text
harness/mcp/README.md
```

내용:

- MCP 이름
- 목적
- 팀 공용/개인
- 필요한 Secret 변수
- 사용하는 환경

초기에는 MCP config generator를 만들지 않는다.

실제 config drift가 반복적으로 문제가 될 때만 나중에 도입한다.

---

# 25. Human + AI 병렬 개발 모델

Harness에 다음 운영 방식을 명시한다.

```text
Human Workspace
→ 다음 Task의 기획/UI/Scene/Prefab/Art 준비

AI Worktree
→ 현재 Task의 Code/Editor Integration/Test
```

사람은 AI 완료를 기다리지 않는다.

단:

```text
Exclusive Assets
```

가 겹치면 동시에 수정하지 않는다.

---

# 26. Orca

Orca는 Harness의 필수 요소가 아니다.

기본:

```text
VS Code / Claude Code CLI
→ Main Agent 1개
```

Orca 사용 조건:

- 여러 독립 Task 병렬 진행
- 여러 Worktree UI 관리 필요
- 한 Agent가 반복 실패한 난제
- 여러 모델의 해결법 비교 가치가 큼

Orca가 Harness를 대체하지 않는다.

---

# 27. 최소 Helper Script

기존 script가 없다면 가능한 범위에서 최소 Helper를 만든다.

```text
create-task.ps1
create-ai-worktree.ps1
remove-ai-worktree.ps1
setup-links.ps1
verify-env.ps1
verify-fast.ps1
verify-unity.ps1
verify-context-budget.ps1
verify-harness.ps1
```

Script는 가능한 단순하게 만든다.

특히 create-task는:

- Task ID 생성
- Task folder 생성
- meta/plan/todo/handoff/log 생성
- branch 이름 제안 또는 생성
- 필요 시 Worktree 생성

정도만 담당한다.

복잡한 Task Management System을 만들지 않는다.

---

# 28. 문서 승격 규칙

Task log에 발견한 정보가 영구적으로 중요하면 적절한 위치로 승격한다.

```text
일시 작업 상태
→ Task

게임 시스템 지식
→ docs/domains

장기 기획 결정
→ docs/decisions

코딩 규칙
→ docs/standards

팀 작업 규칙
→ harness/policies
```

즉 `tasks/`가 프로젝트 지식의 최종 저장소가 되면 안 된다.

---

# 29. 최종 검증

Migration 후 다음을 확인한다.

1. 기존 문서가 삭제되지 않았는가
2. 이동/통합 과정에서 내용 손실이 없는가
3. 기존 링크가 깨지지 않았는가
4. AGENTS.md가 짧은 지도 역할인가
5. CLAUDE.md가 공통 Harness를 재사용하는가
6. Domain Map이 모든 파일 목록이 아니라 핵심 지도인가
7. Human Decision Gate가 Task와 연결되는가
8. Open Decisions가 있으면 AI가 구현을 멈추는 규칙이 있는가
9. Code / Editor Integration이 구분되어 있는가
10. 작은 Task에서 같은 Agent가 순차 실행 가능한가
11. Human / AI Workspace가 분리되는가
12. AI가 Human Unity Editor를 조작하지 않는가
13. Exclusive Assets가 Task와 연결되는가
14. Branch / Worktree 명명 규칙이 명확한가
15. Task 기록이 짧은 bullet 중심인가
16. log.md가 기본 Context에서 제외되는가
17. handoff.md가 짧게 유지되는가
18. Context Budget 검증이 가능한가
19. `.env`가 Git에서 제외되는가
20. 기존 MCP가 깨지지 않았는가
21. Unity 코드/Scene/Prefab에 의도치 않은 변경이 없는가

---

# 30. 최종 보고 형식

작업 완료 후 다음만 간결하게 보고한다.

## Migration Summary

## Final Structure

## Moved

## Merged

## Created

## Preserved

## Policies

## Roles

## Skills

## Task Workflow

## Human Decision Flow

## Worktree Workflow

## Context Budget

## Git Tracked

## Local Only

## Delete Candidates

## Manual Review Required

## How To Start A New Task

마지막으로:

- `git status`
- `git diff --stat`
- 주요 Harness diff

를 확인하고 Harness Migration과 무관한 게임 기능 변경이 없는지 자체 검토한다.

기존 파일은 삭제하지 않는다.
게임 기능은 구현하지 않는다.
이번 작업의 범위는 Harness Migration 및 운영 구조 구축까지다.