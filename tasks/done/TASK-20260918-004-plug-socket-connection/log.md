# Task Log

Append dated implementation details, command outcomes, investigations, and discarded approaches here. This file is historical detail and is not part of default agent context.

## 2026-09-18

- Verified the user-pushed branch: local and `origin/feat/infinity-power-connection` both resolve to `458d805fcfab404b777858973f9f651f654b2688`.
- Created TASK-20260918-004 from the repository template; no branch/worktree created.
- Read-only investigation found `PlugConnector`/`SocketConnector` expose raw one-sided state only; no existing caller pairs or clears both endpoints.
- `PlugDragInput.DragStarted` is available but unused; `CableRoutingController.OnDragEnded` must attempt Socket connection before existing invalid-drop recovery.
- Socket prefab children have `SocketConnector` but no Collider2D. Existing `WallOutlet.sockets` and `PowerStrip.sockets` references are populated. Distance-based discovery avoids unnecessary high-blast-radius prefab edits.
- `InfiniteMode` has no wall-outlet or multitap instance, so Runtime Integration and Human Play Check require human scene setup under the Human Setup Required policy.
- No implementation performed.

- Human confirmed setup: one Wall_Outlet 1-socket prefab placed and positioned under `Connections/Wall_Outlets` in `InfiniteMode`, Scene saved.
- Implemented P0-1D directly on `feat/infinity-power-connection` (code-only change, no scene/prefab/asset edits, so no separate task worktree was created — matches how the prior P0-1C/P0-1C.1 code stages were done on this branch).
- Added `Assets/01_Scripts/Power/Connection/PlugSocketConnection.cs` (+`.meta`, folder `.meta`): static `Connect(plug, socket)` / `Disconnect(plug)` that atomically pair/clear both `PlugConnector` and `SocketConnector` sides. Rejects `Connect` when the socket is occupied by a different plug.
- `CableRoutingController`:
  - Subscribed to `PlugDragInput.DragStarted` to call `PlugSocketConnection.Disconnect` immediately when a connected Plug starts dragging.
  - Added `TryConnectToNearbySocket`, called first in `OnDragEnded`: finds the nearest `SocketConnector` (plain `FindObjectsByType` distance scan — Sockets have no Collider2D by design) within a new `socketAcquisitionRadius` (default `0.25`) Inspector field, rejects if occupied, resolves the nearest walkable cell adjacent to the (wall-blocked) Socket cell via the existing `GridPathfinder.TryFindNearestWalkable`, paths to it with `TryFindPath`, and reuses the existing `BuildWorldPath` to end the route exactly at the Socket's world position. Rejects on `CableLength` overflow. On success: `PlugSocketConnection.Connect`, snap Plug to the exact Socket position, render, update `lastValidPlugPosition`.
  - On any rejection (no Socket in range, occupied, unreachable, or too long) the method returns false and `OnDragEnded` falls through unchanged to the existing P0-1C nearest-valid-position logic.
- Did not add Socket colliders, touch Power/Flow/PowerStrip-movement code, or edit any Scene/Prefab/`.asset` file.
- Verification run: `pwsh -File scripts/verify-fast.ps1` passed (pre-existing handoff-length warnings only, unrelated to this change). No live Unity Editor/Pipeline instance was reachable from this shell to run `verify-unity.ps1` or a compile check; Unity will recompile these scripts the next time the human's Editor regains focus. `NO_PROJECT_TESTS` — no EditMode/PlayMode test assembly exists yet.
- Did not proceed to Power Validation; Task remains open pending Human Verification.

- Human reported `Connections/Wall_Outlets` was actually empty in `InfiniteMode` (prior manual placement did not persist) and delegated one-off AI Setup Allowed for this Task: place a single `Wall_Outlet_One` instance for P0-1D testing only.
- Found a live, ready Unity Editor/Pipeline instance already targeting this same project (`D:\github\Octoplug`, port 7800); used it directly (this is the human's own primary checkout, matching how the earlier code-only stages of this branch were also done in-place — no separate worktree exists for this branch).
- Inspected the scene via `get_scene_hierarchy` and `get_component_properties` (Transform/BoxCollider2D) for the Room, its 4 Unlocked walls, and the Door: Room floor is `size(8,6.17)` centered at origin; walls at `x=-4`(left, has the Door around `y≈-0.3`), `x=4`(right, clear), `y=-3`(bottom), `y=3`(top); all ancestor transforms are at local `(0,0,0)` so these collider offsets equal world coordinates directly.
- Also read the TV's cable geometry (`CableOrigin` world ≈ `(1.29, 0.02)`, `CableLength=3`) so the outlet would actually be reachable for the Human Play Check — the bottom/left walls were too far (>6 units away); the right wall at `y≈0` is ≈2.73 units away, well inside the 3-unit Cable length.
- `instantiate_prefab` → `Assets/03_Prefabs/Wall_Outlets/Wall_Outlet_One.prefab` (created at scene root by default), `set_parent` under `Wall_Outlets` (`world_position_stays=false`), `set_transform` to local/world position `(4, 0, 0)`, rotation `(0, 0, 0)` — this places the Socket exactly on the right wall's centerline (wall spans `x:[3.85,4.15]`), `y=0` is roughly mid-wall, far from both corners (`y≈±3.09`) and from the Door (which is only on the left wall).
- Verified visually with `capture_scene_view`: the outlet's socket hole renders facing left, into the room, i.e. rotation `(0,0,0)` was already correct for this wall — no further rotation needed. Deleted the temporary screenshot file (`Assets/Screenshots/…`) afterward; it was never referenced by anything.
- Re-read `get_scene_hierarchy` to confirm the instance's structure (`WallOutlet` on root, `SocketConnector` on `Sockets/Socket`, `Head/Body` sprite) exactly matches the source prefab — no structural edits made.
- `save_scene` → scene reported `isDirty: false` afterward. `console`/`clear_console` confirmed `compilationFailed: false` and 0 console errors/warnings after all of this Task's C# and scene changes (the only 2 prior console errors were this session's own malformed CLI argument attempts, not a runtime/script issue — cleared).
- Did not touch Wall/Door colliders, other prefabs, or any other Scene object; did not restructure the outlet prefab; no second outlet/multitap system created.

- Human reported the real blocker: dragging the Plug onto the visible Wall Outlet Socket never connects. Investigated on the live, already-`playing` Editor for this project rather than guessing.
- Confirmed Socket placement is correct first (per the report's own checklist), before suspecting the connect logic: used `create_gameobject` (a small cube) at the SocketConnector's exact world position `(4,0,0)` (had to `editor_stop` briefly — scene mutation is blocked in Play Mode) and `capture_scene_view` to compare it against the rendered outlet art. The marker landed right on the visible socket hole (sub-0.1-unit difference) — Socket Transform vs visual center is **not** the problem. Deleted the marker and re-saved (clean) before re-entering Play Mode.
- Used the Pipeline `eval_file` command (Roslyn C# executed live inside the running Editor/Play session — no synthetic standalone harness) to call the actual private `CableRoutingController.OnDragEnded`/`OnDragged` methods via reflection against the real TV `CableRoutingController`/`CableInfo`/`PlugConnector` and the real placed `Wall_Outlet_One`'s `SocketConnector`, so every measurement below is the shipped code running on the real scene objects:
  - Socket world pos `(4.00, 0.00)`; `SocketCell=(16,0)` is `Blocked` (on the wall, as expected/by design); nearest walkable cell is `(14,0)` → world `(3.63, 0.13)`.
  - Calling `OnDragged(pointer = exact Socket position)` — reproducing a real mouse held exactly over the visible socket — left the **Plug** at `(3.63, 0.13)`, `0.395` world units from the Socket. This is `OnDragged`'s existing, correct P0-1C wall-avoidance clamp (a Socket cell is deliberately non-walkable) doing exactly what it's supposed to for a plain drag.
  - `OnDragEnded` then searched for a nearby Socket using `currentPos` = the Plug's clamped transform position (`0.395` away) against `socketAcquisitionRadius = 0.25` — always false, for **any** drop precision, because the 0.395 gap is a fixed consequence of the grid's `cellSize=0.25` and the Socket sitting on a wall cell, not of where the user actually pointed.
  - Routing/length were **not** the blocker: replaying `BuildWorldPath` for Origin→Socket gave a real routed length of `2.94` against `CableLength=3` (comfortably inside budget, matching the ≈2.73 straight-line estimate plus jog overhead).
- **Root cause**: `OnDragEnded`'s Socket search compared against the Plug's own (already wall-clamped) transform position instead of the raw pointer position the human was actually dropping at. Confirmed exactly once by reflection-driving the unmodified code end-to-end (`OnDragged` then `OnDragEnded`) → `Plug.IsConnected=False`, matching the human's report precisely.
- **Fix** (`CableRoutingController.cs` only, minimal): added a `lastPointerWorldPos` field updated at the top of `OnDragged` (before any wall-clamping) and initialized once in `OnDragStarted` as a same-frame-click safety default; `OnDragEnded` now passes `lastPointerWorldPos` (not the Plug's clamped position) into `TryConnectToNearbySocket`. `PlugConnector`, `SocketConnector`, `PlugSocketConnection`, `GridPathfinder`, `CableRoutingGrid`, and the P0-1C plain-drop fallback (which still correctly uses the Plug's actual position) are untouched. `socketAcquisitionRadius` left at `0.25` — not widened; the actual measurement point was the bug, not the radius.
- Recompiled (`recompile` → `recompile_status: completed, compilationFailed:false`) and re-ran the identical `eval_file` reflection reproduction against the fixed code in a fresh Play session: `OnDragged(pointer=socketPos)` still clamps the Plug to `(3.63,0.13)` as before (unchanged, correct), but `OnDragEnded` now reports `Plug.IsConnected=True`, `Socket.IsConnected=True`, Plug snapped to exactly `(4.00,0.00)`.
- Additional runtime regression pass (same `eval_file`/reflection technique, one Play session): grabbing a connected Plug (`OnDragStarted`) disconnects both sides immediately; dragging away and releasing on open floor does not connect and does not corrupt state; releasing at a `~0.14`-unit imprecise offset from the Socket still connects (symmetric `Plug.ConnectedSocket`/`Socket.ConnectedPlug` confirmed both directions); releasing at a `0.4`-unit offset (outside the `0.25` radius) is correctly rejected and falls back to the existing P0-1C nearest-valid-position behavior — no regression. Did not runtime-test "occupied by a different Plug" (only one Plug instance exists in this scene; that path is unchanged code, already reasoned through at implementation time).
- Reset the Plug to its original authored resting position `(0,0,0)`, disconnected, before handing back — so the human's next Play Mode session starts from the same clean state as before, for a single fresh drag/drop check.
- `editor_stop` → scene `isDirty: false` (Play Mode changes never touch the saved scene); `console`/`clear_console` confirmed 0 errors/warnings and `compilationFailed:false` afterward. `pwsh -File scripts/verify-fast.ps1` passed (same pre-existing unrelated warnings as before).
- Did not touch Room/Door/Wall colliders, the Wall_Outlet prefab, TV placement, or Cable Routing UX; did not proceed to Power Validation.

- Human Play Check round 2: connect and grab-to-detach PASS. Reported 2 UX issues: (1) the dragged Plug renders behind Wall Outlet/PowerStrip sprites; (2) grabbing a connected Plug disconnects it immediately, so even a tiny in-place wiggle drops the connection and re-dropping doesn't feel like a real reconnect.
- Confirmed the sorting cause first, not guessed: `Cable.prefab`'s `Plug/Handle` `SpriteRenderer` is authored at `m_SortingOrder: 1` layer 0 — checked `Wall_Outlet_One.prefab`'s `Head/Body` and `Products/TV.prefab`'s `Icon`, both `m_SortingOrder: 1`; per the Connection/Power Domain Map, Base Cable is `-1`, Flow is `0`, Product/Multitap/Outlet sprites are `1`. Handle sits at the *same* order as everything else with no tie-break in its favor, so it can render behind — this matches the report.
- **Fix 1 (rendering)**: `CableRoutingController` now caches `cableInfo.Plug.GetComponentsInChildren<SpriteRenderer>(true)` once in `Start()` (currently just `Handle`; no name hardcoded). `OnDragStarted` saves each one's current `sortingOrder` into a parallel array and sets it to a new `[SerializeField] draggingSortingOrder = 2` (one above Product/Outlet's `1`). `OnDragEnded` calls a new `RestorePlugSorting()` as its very first statement (so it runs on every exit path, including the new retained-connection path below) to put every renderer back to its pre-drag value. Cable Base/Flow `LineRenderer` sorting is untouched — only `SpriteRenderer`s under the Plug are touched, so `Line`/`ElectricEffects` (which are `LineRenderer`, not `SpriteRenderer`) are never affected by the filter.
- **Fix 2 (disconnect UX)**: replaced the old "`OnDragStarted` → immediate `PlugSocketConnection.Disconnect`" with a remember-then-confirm design:
  - `OnDragStarted` now only records `originalSocket = cableInfo.Plug.ConnectedSocket` (may be null) — no state change yet.
  - `OnDragged` (every frame) checks, only when `originalSocket != null`, whether the raw pointer (`pointerWorldPos`, the same "actual drop intent" value fixed for the acquisition bug) has moved more than a new `[SerializeField] socketDetachRadius = 0.4` (Inspector-tunable, intentionally above `socketAcquisitionRadius = 0.25` for hysteresis, as the report itself suggested) from `originalSocket`'s position. Only once that threshold is crossed does it call `PlugSocketConnection.Disconnect` and clear `originalSocket` — exactly once, guarded by the null check, and independent of any Enter/Exit collider history (Sockets have no Collider2D at all; this is a plain distance check against the live pointer position every frame, same style as the existing acquisition check).
  - `OnDragEnded` now checks `originalSocket` first: if still non-null (the whole drag stayed inside the detach radius), the Plug/Socket connection was never touched, so it skips `TryConnectToNearbySocket` and the P0-1C fallback entirely and instead snaps the Plug back to the exact original Socket position via a new shared `TryRouteToSocket`/`SnapPlugToSocket` pair (extracted from `TryConnectToNearbySocket`'s tail so both the "still connected" and "connect to a new/same Socket" paths render the identical routed Cable path). If `originalSocket` is null (never connected, or already detached this drag), behavior is exactly the prior `TryConnectToNearbySocket` → P0-1C-fallback flow, unchanged.
  - `lastPointerWorldPos`-based acquisition from the previous fix is untouched and still feeds both the detach check and the reconnect search.
- Recompiled (`recompile_status: completed, compilationFailed:false`) and ran a full `eval_file` reflection pass on the live Play Mode instance (real `CableRoutingController`/`CableInfo`/`PlugConnector`/`SocketConnector`, not a synthetic model), calling `OnDragStarted`/`OnDragged`/`OnDragEnded` directly in sequence:
  1. Connect at exact Socket pos: still succeeds (`IsConnected=True`, snapped to `(4,0)`); Handle `sortingOrder` elevates to `2` mid-drag, restores to `0` after.
  2. Grab the connected Plug, no movement yet: `Plug.IsConnected` stays `True` immediately (previously this alone caused an instant disconnect).
  3. Wiggle `0.1`→`(-0.05,0.08)` around the Socket (well inside `0.4`), release: still `IsConnected=True`, Plug snapped back to exactly `(4.00, 0.00)`, `Plug.ConnectedSocket==socket` and `Socket.ConnectedPlug==plug` both `True`.
  4. From the still-connected state, drag `0.1` off (still inside `0.4`, stays connected), then to `(1, 1.5)` (`3.35` units away): `Plug.IsConnected` flips to `False` and `Socket.IsConnected` to `False` *mid-drag*, before release. Releasing there leaves it correctly unconnected on open floor.
  5. Grab the now-free Plug, drag back onto the Socket, release: reconnects (`IsConnected=True`, snapped to `(4,0)`) — same-Socket reconnect after a real detach still works.
  6. Final Handle `sortingOrder` back to `0` after the whole sequence.
- Console/`clear_console` confirmed 0 errors/warnings and `compilationFailed:false` throughout. Reset the Plug to its authored resting position `(0,0,0)`, disconnected, and `editor_stop` before handing back (scene reported `isDirty:false` — Play Mode changes never touch the saved scene). `pwsh -File scripts/verify-fast.ps1` passed (same pre-existing unrelated warnings).
- Did not touch Routing/`GridPathfinder`/`CableRoutingGrid`, `CableLength`, Room/Door/Wall colliders, `Cable.prefab`/`Wall_Outlet` prefabs, Power Validation, or Product Powered/Flow. No new Task created.

- Human Play Check round 3: dragging-Plug-above-Outlet PASS. Reported: (1) dropping "very near" the Socket sometimes still fails to (re)connect; (2) sortingOrder falls back below Outlet the instant Mouse Up fires, even on a successful connect, matching the attached screenshot; (3) no immediate visual feedback that a connection succeeded; (4) requested reusing the already-working `CablePowerFlowEffect` as a temporary "Connected" indicator.
- Re-entered the live, `ready` Editor for this project and reproduced Cases A/B/C from the report via `eval_file` reflection against the real `CableRoutingController`/`SocketConnector` (not guesses), reading the actual `lastPointerWorldPos`/`originalSocket`/`socketAcquisitionRadius`/`socketDetachRadius` field values via reflection alongside `Plug.IsConnected`:
  - Case A/B (wiggle within retention, release): worked correctly already — `IsConnected` stays `True` throughout, snaps back exactly. Not the bug.
  - Case C (drag out past `socketDetachRadius=0.4`, then part-way back, release), swept at return offsets `0, 0.1, 0.2, 0.25, 0.3, 0.35, 0.4`: reconnects up to exactly `0.25` (the attach radius), then **fails** at `0.3`, `0.35`, `0.4` — `Plug.IsConnected=False` in every case beyond `0.25`. This is a real, reproducible dead zone: once actually detached, reconnecting to the very Socket you just left required the *tighter* `socketAcquisitionRadius` (`0.25`) rather than the *looser* `socketDetachRadius` (`0.4`) the same drag had just been tolerating a moment before — exactly the "sometimes fails very close to the Socket" the human described.
- **Root cause 1 (connect)**: no hysteresis on the *reconnect* side, only the *retain* side. `TryConnectToNearbySocket` always searched with the tight `socketAcquisitionRadius`, with no memory of which Socket this same drag had just come from.
- **Root cause 2 (sorting)**: `OnDragEnded` called `RestorePlugSorting()` — which always restored to whatever the sortingOrder was *immediately before this drag* — as its literal first statement, before the drop's outcome (connect vs. fallback) was even known. For a fresh drag starting from the authored resting order (`0`, below Outlet's `1`), this restored to `0` even after a successful connect, because "before this drag" and "after a successful connect" are two different states that a single before/after snapshot cannot represent.
- **Fix 1 (reconnect dead zone)**: added `recentlyDetachedSocket` — set to the just-left Socket exactly when `OnDragged`'s detach transition fires (alongside the existing `Disconnect`/`originalSocket = null`), cleared at the start of every new drag (`OnDragStarted`) and the instant any connection succeeds. `TryConnectToNearbySocket` now falls back to checking distance against `recentlyDetachedSocket` specifically (still capped at `socketDetachRadius`, not a global radius increase) only when the normal tight-radius search finds nothing — so a genuinely different, far-away drop still correctly stays unconnected; only "came back to the Socket you just left, within the same forgiving radius" gets the widened check.
- **Fix 2 (sorting timing/levels)**: replaced the single "restore to pre-drag order" scheme with three named, fixed sortingOrder levels — the Plug's actually-authored resting order (captured once in `Start()`, unchanged, `0`), a new `[SerializeField] connectedSortingOrder = 2`, and `draggingSortingOrder` raised from `2` to `3` (so the ordering is Base Cable `-1` < Flow `0` < Product/Outlet `1` < Connected Plug `2` < Dragging Plug `3`, matching the report's suggested structure). `OnDragEnded` was split into `ResolveDragEnd()` (all the existing connect/fallback logic, unchanged) followed unconditionally by a new `ApplyFinalSorting()` that checks `cableInfo.Plug.IsConnected` *after* the drop is fully resolved and sets `connectedSortingOrder` or the authored resting order accordingly — so the topmost dragging order now persists through the entire drop decision, not just until Mouse Up.
- **Fix 3 (temporary Connected feedback)**: `CableRoutingController` now resolves its sibling `CablePowerFlowEffect` via `GetComponent` in `Start()` (no serialized-field wiring needed, no scene/prefab edit) and calls `SetPowered(true)` right where `PlugSocketConnection.Connect` succeeds, `SetPowered(false)` right where `OnDragged`'s real-detach `Disconnect` fires — exactly the two call sites the report specified. Did not rename `SetPowered`/`isPowered`: added an explicit `TODO(Power Validation)` comment at both call sites and in the class doc-comment, plus a new "Temporary (P0-1D): Connected drives Flow directly" section in `docs/domains/connection-power.md` recording that Connected and Powered must be split (Flow shown only when Powered) once House/PowerStrip capacity validation exists.
- Recompiled (`recompile_status: completed, compilationFailed:false`) and re-ran the full sequence via `eval_file` reflection on the live Play Mode instance: connect at exact pos (`sortingOrder`: `3` mid-drag → `2` after connect, not `0`); grab-connected + tiny wiggle + release (`sortingOrder` back to `2`, still connected); drag past `0.4` (real detach, `sortingOrder` stays `3` throughout since still dragging) then return to exactly `0.3` off the same Socket and release (now **reconnects**, `sortingOrder` → `2`) — the previously-broken case; a second real detach followed by a drop far from the Socket (`(2,1.5)`, nowhere near) correctly stays unconnected, `sortingOrder` → `0` (authored resting) — confirms the widened radius is scoped to the just-left Socket only, not a blanket increase; a fresh reconnect from a fully free state still works (regression check). Separately confirmed `CablePowerFlowEffect`'s private `isPowered` field flips `True`/`False` exactly with these Connect/Disconnect calls, and that `flowLine.enabled` follows it once an actual `Update()` tick runs (a synchronous `eval` call has no frame boundary in between, so checking `flowLine.enabled` in the very same call as `SetPowered` is a testing artifact, not a bug — confirmed by invoking the private `ApplyVisibility()` directly and by re-checking on a later, separate command after a real frame had elapsed).
- Console/`clear_console` confirmed 0 errors/warnings and `compilationFailed:false` throughout. Reset the Plug to its authored resting position `(0,0,0)`, disconnected, Flow off, sortingOrder `0`, then `editor_stop` (scene `isDirty:false`, unaffected by Play Mode). `pwsh -File scripts/verify-fast.ps1` passed.
- Updated `docs/domains/connection-power.md`: corrected the now-stale sorting-order note (previous entries said Product/Outlet sprites were `1` with no note about the Plug; now documents the full `-1/0/1/2/3` scheme) and added the "Temporary (P0-1D)" Connected/Powered split TODO. Did not touch Routing/`GridPathfinder`/`CableRoutingGrid`/`CableLength`/Room/Door/Wall colliders/`CablePowerFlowEffect.cs` itself/Power Validation/House Power/PowerStrip capacity or movement/Product overlap alpha fade. No new Task created; did not proceed to Power Validation.

- Human Play Check round 4: basic connect/detach/Flow all PASS. Reported: (1) drops right at/overlapping the Wall Outlet's visible footprint still sometimes fail; (2) the Wall Outlet renders behind the Wall; (3) the Wall itself effectively blocks the Plug from ever physically reaching the Socket's exact position; (4) the outlet isn't mounted parallel to its wall. Confirmed long-term UX: Wall Outlet is a Wall-mounted endpoint, above the Wall visually, rotated to match it; a Socket is a terminal endpoint for Cable routing (fine as the final hop) but never a pass-through to the wall's far side; Room-to-Room Cable movement stays Door-only.
- Investigated on the live, `ready` Editor (not playing) first, since (2)/(4) are pure Editor/prefab facts, not runtime logic:
  - Checked `Room.prefab`'s actual `Wall` `LineRenderer.sortingOrder` values directly (`grep`): walls are `2` or `3` depending on which wall. All 5 `Wall_Outlet_*.prefab`'s `Head/Body` `SpriteRenderer` were still `m_SortingOrder: 1` (the plain Product tier) — confirms (2): Outlet (`1`) sits *below* every Wall (`2`/`3`), so it renders embedded in/behind the wall exactly as reported.
  - Took a `capture_scene_view` of the placed `Wall_Outlet_One` (rotation `(0,0,0)`, unchanged since the earlier placement round): the capsule shape is horizontal, sitting against the *vertical* right wall — confirms (4): the body isn't parallel to its wall, even though its Socket hole happened to face the room correctly (verified in an earlier round).
- **Root cause (rendering)**: Wall Outlet sprites were authored/left at the generic "Product" sorting tier (`1`), which the Room's own `Wall` `LineRenderer`s (`2`/`3`) sit above — nothing in P0-1D had ever raised Outlet above Wall.
- **Root cause (rotation)**: the placed instance's root rotation `(0,0,0)` happens to be the prefab's "outlet body drawn horizontally" orientation; a vertical wall needs the body rotated 90° so its long axis runs along the wall.
- **Fix (rendering)**: set every `Wall_Outlet_{One..Five}.prefab`'s `Head/Body` `SpriteRenderer.sortingOrder` to `4` (above the Wall's max of `3`) via the live Editor (`set_component_properties` on the currently-placed `Wall_Outlet_One` instance, then `apply_prefab_overrides` to push it to `Wall_Outlet_One.prefab`; for `Two`–`Five`, temporarily `instantiate_prefab` → same `set_component_properties` → `apply_prefab_overrides` → `delete_gameobject`, so all 5 share the fix with a genuine one-line-per-file diff, not just the one instance under test). Also raised `CableRoutingController`'s `connectedSortingOrder` (`2`→`5`) and `draggingSortingOrder` (`3`→`6`) so the Plug still renders above the now-higher Outlet in both states — full order is now Base Cable `-1` < Flow `0` < Product `1` < Wall `2`/`3` < Wall Outlet `4` < Connected Plug `5` < Dragging Plug `6`.
- **Fix (rotation)**: `set_transform` on the placed `Wall_Outlet_One` instance to `rotation (0,0,90)` — confirmed via a second `capture_scene_view` (cropped with .NET `System.Drawing` for a close look) that the capsule now runs vertically, flush with the vertical wall, socket-face art unaffected (its sprite is a front-facing outlet icon with no directional "prong" — rotating it does not misalign a facing direction). Left as a scene-instance-only Transform override (not pushed to the prefab default), since rotation is inherently per-instance — which wall an outlet ends up on determines its rotation, not the outlet type. Confirmed the `SocketConnector`'s world position is unchanged by the rotation (its local position is `(0,0,0)`, i.e. exactly the pivot).
- **Root cause (Plug can't reach Socket / near-overlap drops still fail)**: unrelated to rendering — this was a genuine remaining gap in the acquisition logic. `TryFindNearestSocket` only ever measured distance to the Socket's own exact (wall-embedded) position. A first-time connection attempt (not a `recentlyDetachedSocket` reconnect) landing on a walkable cell that is visually right next to/overlapping the outlet — e.g. where the Plug's own wall-avoidance clamp puts it — could still be `> socketAcquisitionRadius` (0.25) from the Socket's exact Transform, so it was rejected even though the user had clearly "dropped on the outlet."
- **Fix (Socket Approach Point)**: added `TryGetSocketApproachCell`/`TryGetSocketApproachPosition` — the nearest walkable cell to a Socket's own cell, searched within a new, deliberately small `[SerializeField] socketApproachSearchRadius = 3` (cells; wall-thickness scale) rather than reusing `nearestValidSearchRadius` (24 cells, room scale) — so this can never tunnel through a wall to a walkable cell in a different Room on the far side. `TryFindNearestSocket` (acquisition) and `TryRouteToSocket` (actual connect routing) both now measure/anchor against this Approach Point in addition to the Socket's exact position, whichever is closer, so hovering near where the Plug will actually end up next to the wall counts as targeting that Socket. The Socket's exact position remains the route's terminal point only (`TryRouteToSocket` still ends with the same final orthogonal jog to it) — nothing routes *through* a Socket to the far side, so Room-to-Room movement is still Door-only, unchanged.
- Recompiled (`recompile_status: completed, compilationFailed:false`) and ran a fresh `eval_file` reflection pass on a new Play Mode session against the real objects: a first-time drop at `x=3.9` and at `x=3.75,y=0.05` (both previously-failing "visually overlapping the outlet" cases, not reconnects) now connect; exact-position connect, wiggle-retain, real-detach-then-reconnect-near-approach-point, and an unrelated far drop after a real detach staying unconnected all re-passed (no regression); a pointer at `x=4.3` — past the wall's far outer edge, i.e. approaching from the wrong/non-room side — correctly stays unconnected and falls back to the same Approach Point via the ordinary P0-1C fallback, confirming wrong-side rejection; `Wall_Outlet_One`'s Body `sortingOrder` read back as `4` at runtime.
- Hit a false alarm mid-verification: grepping the saved scene YAML for `m_Name: TV`/`Wall_Outlet_One`/etc. found nothing and briefly looked like data loss. Root cause was my own misunderstanding, not a bug: prefab instances (TV, Wall_Outlet_One, Room, ...) are stored in `.unity` files as compact `PrefabInstance` + `m_Modifications` diffs against their source prefab, not as flattened `GameObject: m_Name: ...` blocks, so a plain name grep only ever finds objects with an *overridden* name. Cross-checked against the live Editor's `get_scene_hierarchy` (unchanged, full content) and `git status`/`git diff --stat` (only the expected files touched) before concluding nothing was actually lost.
- Console/`clear_console` confirmed 0 errors/warnings and `compilationFailed:false` (the only entries were this session's own earlier CLI-syntax mistakes, not runtime/script errors). Reset the Plug to its authored resting position `(0,0,0)`, disconnected, Flow off, sortingOrder `0`; `editor_stop`; `save_scene` (the new serialized fields on `CableRoutingController` briefly marked the scene dirty on domain reload — expected, harmless, saved clean). Deleted all temporary screenshot/crop files afterward. `pwsh -File scripts/verify-fast.ps1` passed.
- Updated `docs/domains/connection-power.md`: extended the sorting-order note to the new `-1/0/1/2-3/4/5/6` scheme, and added a "Wall Outlet mounting principle (long-term, confirmed)" section recording Wall-mounted-endpoint sorting/rotation/no-corner-placement and the Socket-is-terminal-not-pass-through/Approach-Point design, cross-referenced with the existing Door/Room routing principle.
- Did not touch `GridPathfinder`'s 4-direction rules, `CableLength`, Door routing/Room colliders/Door Collider placement, Product placement, Power Validation, House/PowerStrip capacity, Product Powered state, overlap alpha fade, or Room procedural generation. No new Task created; did not proceed to Power Validation.

- Human Play Check round 5 (final): confirmed all round-4 items PASS (connect/disconnect, Wall Outlet as Wall-mounted endpoint, Plug reaching Socket through the Wall clamp, Connected/Dragging Plug sorting, Door-only room crossing, Power Flow connection feedback). Explicitly accepted remaining visual/feel gaps as sufficient for current demo scope and approved closing this Task as **DONE** rather than iterating further, moving to a new Task (P0-1E) for Power Validation.
- Recorded the Deferred items the human asked not to lose in both this Task's `meta.md` and a new "Deferred TODO" entry in `docs/domains/connection-power.md`: the acquisition/detach/approach-search radii and Plug sortingOrder values are feel-tuning placeholders, not final; Wall Outlet rotation-to-match-wall is manual per instance with no procedural helper yet; the wrong-side-of-a-shared-wall rejection was never validated against an actual second Room (none exists in this scene); Product overlap alpha fade remains unimplemented (carried from P0-1C.1).
- Moved this Task folder from `tasks/active/` to `tasks/done/` per `harness/policies/task-lifecycle.md` (folder move only, no history rewrite). Created `TASK-20260918-005-power-validation-product-info` for P0-1E via the `create-task` procedure. Did not proceed automatically into P0-1E implementation beyond what the next message explicitly scopes.
