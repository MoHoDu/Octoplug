# Log — TASK-20260918-005

2026-09-18 — Task created via `create-task` after TASK-20260918-004 was closed DONE per human approval (round 5). Investigated (partly via a research fork, partly via direct live-Editor reads) all structural unknowns: confirmed no top-alert UI and no PowerStrip instance exist in the scene; confirmed real icon asset paths/filenames differ from the literal spec (`Assets/02_Resources/Art/UI/`, snake_case); confirmed `UI_ProductTooltip` has exactly 5 sections (Service/Power/Cable/Capacity/Duration) with Power already carrying both an Icons pool and a Label — resolving an apparent discrepancy between the research fork's summary and an earlier direct hierarchy read.

2026-09-18 — Verified `icon_electric.png` (already used) is a proper colored icon matching current UI style — kept as-is. Confirmed the `_filled` icon set (fun/food/cold/hot/plug/capacity/timer) are white silhouettes meant to be tinted via `Image.color`, same convention as the existing `icon-person.png` in `UseInfo` — applied a dark-gray placeholder tint, flagged as an art decision.

2026-09-18 — Implemented: `UsageType` flags enum; extended `ApplianceSource` with UsageTypes/Capacity/SatisfactionDurationSeconds/IsPowered; `PowerValidationService` (House + PowerStrip, recompute-based); hooked Power Validation into `CableRoutingController.TryConnectToNearbySocket` (validate before Connect, reject leaves no stale refs) and the real-detach branch of `OnDragged` (Powered=false + Flow off); replaced the old bare Connected==Flow wiring. Updated the Domain Map's "Temporary Connected drives Flow" section to the now-confirmed Connected/Powered architecture.

2026-09-18 — Implemented UI layer: `UsageTypeIconLibrary` (ScriptableObject asset, 4 entries), `ProductInfoView` (icon-count Power row + pooled multi-tag row), `UseInfoView` (pooled active/waiting rows, hide-at-zero), `ProductClickInput` (passive click target) + `PlugDragInput.IsPointerOverAnyPlug` (new static helper, Plug-drag priority) + `ProductTooltipController` (single input owner: routes clicks to Product/empty-space/Plug). Wired all 5 Product prefabs and `UI_ProductTooltip.prefab` via `PrefabUtility.LoadPrefabContents`/`SaveAsPrefabAsset`. Set test placeholder values (House=10W; per-product wattage/usage/capacity/duration).

2026-09-18 — Live-Editor verification (Play mode + `eval` reflection): found and fixed a real bug in `UseInfoView` — `Transform.Find` only located one of each same-named `Using_Person`/`Icon` sibling pair, leaving the other untracked; fixed by collecting all same-named siblings at Start. Found and correctly diagnosed two test-only false alarms (not real bugs): `CablePowerFlowEffect`'s `Update()`-deferred visibility application reading stale immediately after a synchronous `SetPowered` call (fixed by invoking `ApplyVisibility` directly in the test), and a runtime-`Instantiate`'s `Start()` not having fired yet within the same synchronous eval call (fixed by invoking `Start` via reflection in the test). After fixes, verified: connect/disconnect/reject-under-budget/reconnect-after-restore all correct with zero stale state; ProductInfo/UseInfo counts and icons correct on all 5 products; Tooltip show/position(below-top-half, above-bottom-half, corner-clamp)/data-binding/hide correct; Product-click hit detection and Plug-drag priority correct. Zero Console errors (2 expected warnings from the intentional rejection tests). Scene saved with House=10W.

2026-09-18 — Task marked implementation-complete, HUMAN_VERIFY_REQUIRED. Not proceeding to any further stage per explicit instruction.

2026-09-18 — Round-1 Human Verification result: **PASS** on House-limit connect/reject, Powered/Flow, ProductInfo/Tooltip basic data, Tooltip open/close. 7 UI/UX corrections requested (not a re-open of the underlying power logic): (1) ProductInfo power display → single icon + number, not N icons; (2) ProductInfo overall alpha 0.8 (power + tag only, not the Product sprite, not UseInfo); (3) Tooltip panel scaled to ~70%; (4) Tooltip positioning must never overlap the Product's actual visual bounds, with opposite-side retry if clamping would force one; (5) Tooltip's Power row needed its lightning icon restored (was fully hidden in round 1, relying on the Label alone); (6) a power-rejected connection's Plug fallback position needed to be pushed further from the Socket so it no longer visually overlaps the Outlet; (7) the Tooltip must close the instant any Plug starts a drag, not just on empty-space clicks.

2026-09-18 — Implemented all 7: `ProductInfoView` gained a `powerValueLabel` (new `Value` TMP object added under each product's `ProductInfo/Power`, GridLayoutGroup replaced with a HorizontalLayoutGroup so icon+label sit side by side) and a `CanvasGroup`-based `displayAlpha` (0.8). `ProductTooltipController.PositionPanel` rewritten to compute the Product's `SpriteRenderer` bounds in screen space and retry the opposite side on a forced-overlap clamp; `Background`'s `localScale` set to 0.7; the Tooltip's Power Icons pool's first icon re-shown standalone (`powerIcon` field) alongside the Label. `CableRoutingController` gained `powerRejectFallbackDistance` + `ApplyPowerRejectFallbackPosition`/`TryFindPowerRejectFallbackCell` (a ring-expanding BFS from the Socket's own cell, filtered by minimum distance and cable-length/path validity, applied only on the Power-Validation-failure branch — no other invalid-drop path is touched). `PlugDragInput` gained a new static `AnyDragStarted` event (raised alongside the existing per-instance `DragStarted`), subscribed by `ProductTooltipController.Hide` — a narrow, single-purpose addition, not a general event bus.

2026-09-18 — Round-2 live-Editor verification (Play mode + `eval` reflection): ProductInfo shows exactly 1 active icon + correct numeric label (verified "5" for TV) with CanvasGroup.alpha=0.8; Tooltip panel confirmed at 0.7 scale (406×105 screen px, was 580×150), power icon active, and the resulting placement confirmed non-overlapping against TV's actual screen-projected sprite bounds; `AnyDragStarted` invocation confirmed the Tooltip closes immediately; a House-budget-forced rejection confirmed the Plug lands 0.64 units from the Socket (≥ the 0.6 minimum) while staying Disconnected; full connect/disconnect/Powered/Flow regression re-confirmed correct. Zero Console errors throughout. Scene re-saved.

2026-09-18 — Task remains HUMAN_VERIFY_REQUIRED, **not** marked DONE, per explicit instruction. Not proceeding to any further stage.

2026-09-18 — Round-2 Human Verification result: 4 issues reported, scope explicitly limited to ProductInfo/Tooltip UI only (Cable Routing untouched). (1) Tooltip's Power display spec reversed: user now wants pure icon-count (repeat the lightning icon `PowerConsumption` times), not the icon+number combo from round 2 — the numeric Label must be hidden/unused. (2) ProductInfo's overall size looked too large. (3) ProductInfo's power icon was missing/not rendering. (4) The number and Usage icon visually overlapped.

2026-09-18 — Root-caused (3)+(4): round 2's prefab edit removed `ProductInfo/Power`'s `GridLayoutGroup` and added a `HorizontalLayoutGroup`, which reset every Power icon's `RectTransform.sizeDelta` to `(0,0)` — an invisible icon. Separately, the `ProductInfo` root's `HorizontalLayoutGroup.spacing` (`-5`, originally tuned for the old multi-icon overlapping display) pulled the new Power content directly into Tag's own box, causing the reported overlap. (2)'s "too big" perception traced to these same two bugs rather than any actual size increase — root/Power `sizeDelta` values were never enlarged.

2026-09-18 — Fixed: `ProductTooltipController` reverted to an icon-count Power display (`powerIconRow`, toggled by `min(watts, pool size)`), numeric `powerLabel` hidden at `Awake`; `UI_ProductTooltip.prefab`'s Power icon pool restored to all-5-active baseline. `ProductInfoView` gained a self-healing `powerIconSize` guard (re-asserts the visible icon's `sizeDelta` every refresh, closing off this exact bug class from recurring); all 5 Product prefabs' Power icons had `sizeDelta` restored to `(10,10)`, root `HorizontalLayoutGroup.spacing` changed `-5`→`+2`, and the `Value` label given TMP auto-sizing + Truncate overflow to guard against future text bleed.

2026-09-18 — Round-3 live-Editor verification (Play mode + `eval` reflection): Tooltip Power icon-count confirmed correct at 1/3/5/8 watts (1/3/5/5-clamped icons, numeric label inactive); ProductInfo's active icon confirmed `sizeDelta=(10,10)` (self-healed) with zero world-space `RectTransform` overlap between Tag and both the Power icon and the Value label on TV; all 5 products' ProductInfo re-verified (1 active icon + correct numeric value + alpha 0.8 each); full connect/disconnect/Powered/Flow regression re-confirmed correct. Zero Console errors. Scene re-saved. Cable Routing code was not touched this round.

2026-09-18 — Task remains HUMAN_VERIFY_REQUIRED, **not** marked DONE. Not proceeding to any further stage.

2026-09-18 — Discovered (via `git log`) that the user directly redesigned several Product/UI Prefabs in the Unity Editor between rounds and committed the result (`d057daf "resources: Changed art and prefab design"`, `ca3a87f "resources: Fixed sprites order"`) — `git status` no longer shows `TV/Fan/Heater/Induction/Air_Conditioner.prefab` or `UI_ProductTooltip.prefab` as modified because their current on-disk state IS the new committed baseline. The user's message explicitly designated this current Prefab state as Source of Truth and instructed against reverting/redesigning RectTransform/Scale/Position/Spacing/Layout/Icon size/Sprite/color/Hierarchy/placement/Sorting Order, and against any further "code forces a value onto the Prefab" pattern (naming round 3's own `powerIconSize`/similar as the exact anti-pattern to remove).

2026-09-18 — Re-read the actual current Prefab state fresh via live-Editor hierarchy dumps (not relying on task history): confirmed `ApplianceSource`/`ProductInfoView`/`ProductTooltipController`/`ProductClickInput` component wiring survived the user's redesign intact (same GameObject names, same serialized references), but visual values changed extensively (Power icon pool now correctly sized at authored `(10,10)` with 5 real icons in both `ProductInfo` and `UI_ProductTooltip`; `ProductInfo/Power/Value` already manually deactivated by the user; Tag/Service icon colors now specific user-authored tints; a new `Background` sibling with its own `ProductClickInput`+Collider2D added to each Product; Product `Icon`=11/`Background`=10 sortingOrder; `ProductInfo`/`UseInfo` Canvas sortingOrder still the unset `0`). Also confirmed Heater's `PowerConsumptionWatts=6` (my own round-1 placeholder) now exceeds the current 5-icon pool.

2026-09-18 — Removed the round-3 anti-pattern entirely: `ProductInfoView.tagIconTint`/`powerIconSize` and `ProductTooltipController.serviceIconTint` deleted along with their forced-value assignments — both classes now only toggle active state, set `.sprite`, and read data, never `RectTransform`/color/scale. `ProductInfoView.RefreshPowerRow` reverted to toggling the full icon pool by count (matching the user's own Prefab, which already has 5 real icon slots, not the round-3 icon+number layout). Added an icon-pool-overflow `Debug.LogWarning` (naming product/needed/available) to both `ProductInfoView` and `ProductTooltipController`, so a mismatch like Heater's is reported, never silently presented as correct. Raised `ProductInfo`/`UseInfo` Canvas `sortingOrder` `0`→`100` on all 5 Products (confirmed root canvases, so this value applies directly; `UI_ProductTooltip`'s `ScreenSpaceOverlay` Canvas needed no change). Added `CableRoutingController.EffectiveDraggingSortingOrder()` — reads the owning Product's own authored `Icon`/`Background` sortingOrder once in `Start()` and raises the dragging Plug's order only as far as needed to clear it, never below the authored `draggingSortingOrder` floor and never introducing a new hardcoded absolute number; `connectedSortingOrder` left untouched (no reported issue there).

2026-09-18 — Round-4 live-Editor verification (Play mode + `eval` reflection): TV's ProductInfo/Tooltip both confirmed showing exactly 5 active icons (5W) with no number anywhere; Tag color confirmed still the user's authored teal (not the old forced gray); Service icon color confirmed still the user's authored tint; CanvasGroup alpha still 0.8; `ProductInfo`/`UseInfo` Canvas sortingOrder confirmed 100; Heater(6W) confirmed clamped to 5 active icons with the expected Console Warning; dragging Plug's Handle sortingOrder confirmed raised from would-be 6 to 12 (above TV's own Icon=11) and confirmed it returns to its normal resting/connected values after the drag ends; full connect/disconnect/Powered/Flow regression re-confirmed correct. Zero Console errors (10 total warnings — all expected: prior rounds' intentional rejection tests plus this round's new overflow warning plus one pre-existing unrelated Sprite-tiling import warning). Scene re-saved. Cable Routing was not touched or started this round.

2026-09-18 — Domain Map updated with the durable UI↔code contract confirmed this round (Prefab is source of truth; code never sets layout/color/size; Power display is icon-count-only; sortingOrder scheme) and a new Deferred TODO entry for the Heater icon-pool mismatch. Task remains HUMAN_VERIFY_REQUIRED, **not** marked DONE. 8-direction Cable Routing was not started.

2026-09-18 — Round-4 Human Verification result: **PASS** ("해당 작업 사용자 검증 성공. 완료."). Task closed **DONE**. Folder moved to `tasks/done/`. Open Human Setup Required items (top-alert UI, PowerStrip real instance, Heater icon-pool-vs-wattage mismatch) preserved in `docs/domains/connection-power.md`'s Deferred TODO and this Task's `meta.md`. Superseded by newly-created TASK-20260918-006 (Grid Spatial Foundation + 8-Direction Cable Routing), created via the `create-task` procedure.
