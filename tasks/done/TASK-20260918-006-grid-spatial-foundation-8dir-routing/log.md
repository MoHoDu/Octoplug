# Log — TASK-20260918-006

2026-09-18 — Task created after TASK-20260918-005 closed DONE (human verification PASS). User authorized investigation → plan → implementation → automated verification in one pass, no mid-way approval needed unless a new Human Decision Gate item arose (none did).

2026-09-18 — Investigated `CableRoutingGrid`/`CableRoutingGridService`/`GridCellState`/`GridPathfinder`: confirmed the existing Grid is already the right abstraction level (`WorldToCell`/`CellToWorld`/`GetState`/`IsWalkable`) and extended it in place rather than building a parallel system.

2026-09-18 — Investigated the reported LineRenderer straightness/width complaint by dumping every relevant property (alignment, widthCurve, widthMultiplier, textureMode, numCornerVertices, transform/parent lossyScale) on TV's live Cable instance and its `Cable.prefab` source. Root cause found: Flow (`ElectricEffects`) used `Alignment=Local` while Base (`Line`) correctly used `Alignment=View` — Local alignment fixes the width ribbon to the Transform's local up-axis, so it is only perpendicular to the segment for one specific direction, explaining inconsistent width between differently-oriented segments (and would have been far worse with diagonals). Also found both LineRenderers' `widthCurve` was a malformed single off-center keyframe (an Editor authoring artifact).

2026-09-18 — Implemented: rewrote `GridPathfinder.TryFindPath` from 4-direction uniform-cost BFS to 8-direction weighted A* (octile heuristic, diagonal cost √2) with diagonal corner-cutting prevention built into the neighbor expansion; extended `TryFindNearestWalkable`/`TryFindNearestValidDropCell` and `CableRoutingController`'s power-reject fallback search to 8 directions for consistency. Confirmed (no changes needed) that Cable Length accounting, Path Simplification, and Base/Flow shared-path rendering were already diagonal-correct by construction (Euclidean-distance-based, direction-agnostic dot-product collinearity check, and a single shared point list respectively). Extended `CableRoutingGrid` with a `CellEdge`/`CellEdgeWorld` utility and an object-placement occupancy API (`SetObjectOccupied`/`IsObjectOccupied`/`IsFreeForPlacement`) independent of Cable-traversal `IsWalkable` — purely additive, nothing currently calls it. Fixed `Cable.prefab`'s Flow alignment (`Local`→`View`) and normalized both LineRenderers' `widthCurve` to a clean flat 2-key curve at the identical evaluated value — zero visual/width change, Sorting Order untouched.

2026-09-18 — Automated verification: synthetic-grid tests (no scene dependency) for World↔Grid conversion, diagonal/horizontal/vertical/mixed shortest-path costs (numerically verified against expected octile values), corner-cutting rejection (direct diagonal cut correctly rejected in favor of an orthogonal detour), full-wall blocking, and Door-only Room crossing — all passed. Scene-level regression via real TV drag: connect/disconnect/Powered/Flow, Power-Validation-reject-then-fallback-position (still ≥0.6 units from Socket), far-side-of-wall Socket endpoint non-pass-through, Base/Flow identical rendered path (point-for-point), Flow alignment now matching Base, and a diagonal-heavy drag beyond Cable Length correctly clamped to exactly the authored length (3, verified via actual rendered polyline length, not an approximation) — all passed. Zero Console errors.

2026-09-18 — Domain Map updated with the confirmed durable facts (8-direction weighted routing + corner-cutting rule, Grid extension decision, LineRenderer alignment fix, object-placement occupancy API, Room Generation data contract), kept short per Context Budget. No Prefab visual design was touched. Task remains HUMAN_VERIFY_REQUIRED, not marked DONE. Not proceeding to any further stage.

2026-09-18 — Round-1 Human Verification result: PASS on 8-direction routing, Door-only Room crossing, no diagonal wall-corner cutting, and Power Flow following Base Cable — but FAIL on Cable geometry: "일부 상황에서 이상한 궤적으로 짧게 여러 번 꺾인다," specifically near a Wall Outlet Socket's approach (small zig-zag/hook), plus inconsistent width on some short corner segments. Explicit instruction: fix via render-path post-processing only, not by touching 8-direction A*, weighted cost, corner-cutting, Door crossing, Grid cellSize, or Power/Connection logic; investigate the actual geometry (raw/simplified/final coordinates) before assuming a cause.

2026-09-18 — Measured a real TV→Wall_Outlet_One connection attempt's raw grid-cell path, pre-simplification world path, and final LineRenderer positions via `eval`. Found the actual root cause: `CableRoutingController.BuildWorldPath`'s cell-append loop ran only `1..cellPath.Count-2`, excluding the *last* cell, which was instead appended via `AppendOrthogonalJog` (the same treatment meant only for the two genuine sub-cell endpoint gaps). Once 8-direction routing made the final step into a Wall Outlet's Approach Point a legitimate diagonal, this bug rewrote that one clean diagonal segment into a forced 2-segment orthogonal L-bend on every single Wall Outlet connection. Fixed by appending every cell (including the last) directly. Re-measuring confirmed the fix: the previous 2-point L-bend became one clean 0.354-length (`0.25×√2`, exactly one diagonal grid cell) 45° segment.

2026-09-18 — A residual short "hook" remained: the sub-cell gap between the Approach Point and a Socket's *exact* mount position (often ~0.13 units on both axes, roughly half a grid cell) still produced a visible short bend via `AppendOrthogonalJog`. First attempt: a generic post-hoc "merge short segments near either polyline end" pass added to `CablePathRenderer`. Verification (checking every segment's angle against the 8 canonical directions) caught this approach merging two *different* canonical-direction interior segments (a 45° diagonal + a -90° vertical) into a single non-canonical 27° segment for a short overall path — violating the explicit "8-direction alignment everywhere except the Socket terminal" requirement. Reverted. Replaced with a surgical fix: `AppendOrthogonalJog` itself now skips inserting its intermediate bend when either axis gap is below a new `minRenderSegmentLength` (0.15) threshold, drawing one direct segment instead — since this method is only ever called for the two genuine sub-cell endpoint gaps (never an interior A* cell, after the previous fix), it cannot corrupt any interior segment's direction. Re-verified across horizontal/vertical/diagonal drags: every interior segment stayed exactly 8-direction-aligned; only the two endpoint regions (Origin's and, when present, a Socket's) show a single non-canonical-angle segment, as intended.

2026-09-18 — While investigating, the TV→Wall_Outlet_One connection was found to fail outright (Power Validation passed fine; it never got there — the Cable-length check rejected it). Root-caused: `GridPathfinder.TryFindNearestWalkable`'s BFS-queue-order search only guarantees fewest hops, not true nearest-by-distance, once diagonal neighbors are mixed into the same queue — with 8 directions this could (and, before this fix, did in some configurations) return a farther cell than a proper distance search. Rewritten to an exhaustive true-nearest scan (small radius, cheap). Verified this specific case's Approach Point result was unchanged by the fix — the actual cause of the length failure was separate and unrelated to any code in this Task: **TV's own placed position moved during the user's committed Prefab/scene redesign** (now `(0.76, -0.19)`, was `(2, -0.15)` throughout TASK-005), and the straight-line distance from that new position to `Wall_Outlet_One`'s Socket (`3.246`) already exceeds `CableLength=3` before any pathfinding runs — confirmed by temporarily moving TV closer (Play-mode-only, non-persisted), which connected instantly and cleanly with the fixed render path. This is a stale test-placeholder-value-vs-redesigned-layout mismatch, not a bug in this Task's scope; not fixed here (would mean touching `CableLength` or a Prefab position, both out of bounds) — recorded as Human Setup Required instead.

2026-09-18 — Discovered and fixed a stale-serialization gotcha along the way (same class of issue as TASK-005's `powerIconSize` lesson): newly-added `[SerializeField]` fields (`CablePathRenderer.minSegmentLength` during the discarded approach, then `CableRoutingController.minRenderSegmentLength`) got "baked in" to the scene's TV instance at whatever C# default existed the first time the Editor domain-reloaded after the field was added, and did not pick up a later change to that default. Fixed by explicitly writing the intended value onto both the `Cable.prefab` source and the scene's already-diverged TV instance.

2026-09-18 — Round-2 automated re-verification: synthetic-grid pathfinding tests unchanged/still passing; horizontal/vertical/diagonal drags all show correct 8-direction-aligned interior segments with only the (expected, exempted) Origin-side endpoint correction at a non-canonical angle; a real in-range connection (temporary TV move) shows a single clean terminal segment into the Socket with no hook; Cable Length still clamps to exactly the authored value; Base/Flow still share the identical path. Zero Console errors. Domain Map and Task docs updated. Task remains HUMAN_VERIFY_REQUIRED, not marked DONE. Not proceeding to any further stage.

2026-09-18 — Round-2 Human Verification result: **PASS** on 8-direction Cable Routing, Door crossing, zig-zag/unnecessary-bend resolution, Cable thickness consistency, and Base/Flow identical path. One remaining item: Power Flow's animated direction was backwards — currently `CableOrigin (Product/PowerStrip body) → Plug/Socket`, needed `Socket/Plug → CableOrigin` (supply side → consuming body), matching real-world power direction (Wall Outlet → Product Plug → Product; Wall Outlet → PowerStrip Plug → PowerStrip Head; PowerStrip Socket → Product Plug → Product). Explicit instruction: fix only the Flow animation direction — reuse the existing `CablePowerFlowEffect` structure, do not reverse the Routing Path's point order (used for real Cable geometry), do not touch Base Cable geometry, keep Flow sharing the identical Base points, prefer flipping the UV scroll/texture-offset direction, and apply the same rule uniformly (no per-Prefab/per-Product hardcoding).

2026-09-18 — Fixed: `CablePowerFlowEffect.Update()`'s `scrollOffset -= scrollSpeed * Time.deltaTime` changed to `+=`. Per the LineRenderer's own Tile UV.x convention (0 at the Origin end, max at the Plug/Socket end), increasing the offset visually slides the pattern toward decreasing UV — Plug/Socket → Origin — exactly the requested direction. This is a pure sign flip with no other logic touched; direction-agnostic (reads no Product/Outlet identity), so it applies uniformly to every Cable.

2026-09-18 — Round-3 verification (Play mode + `eval` reflection, temporary Play-mode-only TV move to reach connectable range as before): confirmed `scrollOffset` now increases per frame (was decreasing); confirmed Base and Flow `LineRenderer`s still render the exact identical point list (geometry untouched); confirmed Flow still only becomes visible once genuinely `Powered` (`ApplyVisibility` unchanged). Synthetic-grid pathfinding tests re-run and still passing (regression check — this fix touches an unrelated file). Zero Console errors. Domain Map updated with the confirmed Flow-direction rule (Socket/Plug → CableOrigin, kept short). Task remains HUMAN_VERIFY_REQUIRED pending the final single-item confirmation; per the user's own instruction, a PASS there is sufficient to close this Task DONE. Not proceeding to any further stage.

2026-09-18 — **Round-3 (final) Human Verification: PASS.** User confirmed the full checklist: Wall Outlet → Product Power Flow direction correct, 8-direction Cable Routing normal, Door movement normal, no corner cutting, unnecessary zig-zag resolved, Cable thickness visually normal, Base Cable / Power Flow identical path. User instructed final Task closure per Harness Task Completion / Git Workflow procedure.

2026-09-18 — Ran a final pre-closure regression pass covering the user's full requested checklist "가능한 범위에서": re-ran synthetic-grid pathfinding tests (World↔Grid conversion, diagonal/horizontal/vertical/mixed octile costs, corner-cutting rejection, wall blocking, Door-only crossing) — all still passing. Entered Play mode and re-verified, via a real connection (temporary, non-persisted TV reposition, same technique as prior rounds — TV's authored scene position remains outside `CableLength` of `Wall_Outlet_One`, a pre-existing condition unrelated to this Task, already flagged under Human Setup Required): Socket Connect (Powered=True), Base/Flow LineRenderer positions point-for-point identical, Flow visibility correctly gated on genuine Powered state, and Power Flow direction (`scrollOffset` delta positive per frame, confirming Socket→CableOrigin). Checked Console: 0 errors; 13 warnings present, all pre-existing/expected (repeated House power-limit rejections from test drags exceeding the demo's authored budget, one Sprite Mesh Type/Full-Rect import notice, the carried-over Heater icon-pool-vs-wattage mismatch, and the carried-over authored-Plug-off-grid notice for one Product) — none newly introduced. Exited Play mode; saved the scene with no outstanding changes (all verification moves were Play-mode-only and non-persisted).

2026-09-18 — Closed TASK-20260918-006 as **DONE**. Updated `todo.md` (checked off round-3 Human Verification, added the final regression-pass section, updated the Human Verification section to PASS — all pre-existing Deferred/Not-Started items preserved unchanged), `handoff.md` (Status → DONE, added the final Human Verification PASS record and the final regression-pass detail), and `meta.md` (Status → done, Current Stage → done, Verification State appended with the round-3 PASS and final regression-pass summary). Confirmed `docs/domains/connection-power.md` already contains the durable facts from all three rounds (Grid extension, 8-direction A*/corner-cutting, render-path endpoint-correction principle, Flow-direction rule) — no further Domain Map update needed for closure. Moving the Task folder from `tasks/active/` to `tasks/done/` next, per the user's explicit instruction, followed by the Git wrap-up (status → diff review → commit → push → HEAD verification) and a concise completion report. Not auto-starting any further feature per the user's explicit final instruction.
