# Task Log

Append dated implementation details, command outcomes, investigations, and discarded approaches here. This file is historical detail and is not part of default agent context.

## 2026-09-20

- Created TASK-010 on `feat/resident-demand` from integrated local `dev` at `a22b105`.
- Recorded preparation-only scope and stopped before implementation as requested.
- Fetched remote refs: feature `a22b105` is one commit ahead of `origin/dev` `2f89045`; no merge was required.
- Reconciled TASK-010 with the expanded Resident Satisfaction/Patience, Global Satisfaction, and EXP boundary scope; compacted task context from COMPACT_REQUIRED to WARN.
- Implemented Phase A core plus pure deterministic assignment planning and an actual-change `ApplianceSource.PoweredChanged` boundary.
- Resident Demand Core Release tests: 31/31 PASS, covering local import, Demand progress, deterministic assignment, and Global Satisfaction/EXP boundaries.
- Read-only subagent review could not run because the service returned HTTP 429; no review result was fabricated.
- Added Phase B Unity controller/adapter source with stable request sequences, authoritative connection/power/capacity snapshots, topology/power/content event reassignment, and read-only Product usage snapshots.
- Corrected integration issues during local inspection: initialize only after Room Generation, avoid calling Room Generation initialization, preserve waiter order across recomputes, publish per-frame Resident progress changes, validate authored Capacity instead of coercing it, and use zero-padded hierarchy IDs.
- After the matching Editor opened, fixed the reported `UsageTypeMapperTests` namespace compile error, imported generated `.meta` files, and forced a clean Unity recompile. Ground-truth console reports 0 current errors/0 warnings.
- Focused Unity EditMode tests pass: ResidentDemand 2/2 and Product power event 1/1. Full EditMode regression suite passes 26/26 with no skipped/inconclusive tests. Pure Release tests remain 31/31 PASS; `git diff --check` passes; task context is WARN only.
- Added official controller-level EditMode fixtures using runtime-created Product/Cable/Plug/Socket objects and `SerializedObject`, with no scratch verifier. The tests cover immediate power and topology reassignment, capacity overflow, stable FIFO promotion, unrelated UsageType isolation, one-Product-per-Resident, capacity invariants, and ProductUsageSnapshot availability transitions.
- Phase B verification passes: focused ResidentDemand 5/5, full EditMode 29/29, Resident Demand Core 31/31, Unity compile, `verify-unity.ps1`, and `git diff --check`. Controller verification added no console errors/warnings; the full suite emitted three pre-existing Room Generation missing-hatching warnings. Phase B recorded DONE before Phase C.
- Phase C added `ResidentCardView`, canonical single-Need reverse mapping, an event-driven `ResidentDemandUiCoordinator`, and snapshot-only `UseInfoView`; Product icon cloning was removed and authored pool overflow now clamps with one warning.
- Through the matching Editor, wired `UI_ResidentCard.prefab` and production `InfiniteMode.unity`; confirmed `UseInfo.prefab` references remained valid and preserved its authored hierarchy/pool.
- The first 16-test run timed out client-side but Unity completed 15/16. Result XML identified the failing coordinator event fixture. The fixture now explicitly calls the coordinator's public idempotent `Subscribe()` because EditMode does not reliably invoke MonoBehaviour lifecycle callbacks; production still subscribes via `OnEnable`.
- Phase C verification passes: focused Resident Demand EditMode 16/16, full EditMode 40/40, Resident Demand Core Release 31/31, Unity compile, `verify-unity.ps1`, and `verify-fast.ps1`. Task context was compacted after `COMPACT_REQUIRED`.
- Final task-context check reports TASK-010 `OK` (overall `WARN` is from TASK-008), and `git diff --check` passes with line-ending notices only. Production reinspection confirmed the coordinator and UseInfo serialized references.
- `verify-harness.ps1` fails on its blanket Unity-content diff guard because TASK-010 intentionally changes claimed `Assets` files; no bypass was attempted. Final Unity ground truth is 0 errors/5 warnings, but persisted console history prevented attribution of the current warning delta and console clearing was denied.
- Phase C follow-up added explicit Enabled authoring, room-15 two-Need rows, Available Usage-aware no-fallback selection, gameplay-room Product discovery, multi-Usage aggregation, idle retry on normal refresh, and debug inspection controls while preserving Capacity/FIFO/snapshot assignment.
- Updated Resident Card binding for Icon01/Icon02/None/check and red Patience versus authored-green Satisfaction. Updated UseInfo to one ordered 20-slot pool; legacy references migrate and safely identify their common parent without cloning.
- Matching Editor saved the Resident Card prefab and `InfiniteMode` scene with the canonical `icon-check` Sprite; validated UseInfo has exactly 20 direct Image slots and shared authored color references.
- Final follow-up verification: Core 33/33, controller 7/7, UI 8/8, room regression 2/2, full EditMode 46/46; C# compile 0 warnings/errors; Unity/fast/diff checks pass. TASK-010 context is OK; overall context WARN remains TASK-008. Harness still fails only on the generic intentional-Assets guard.
- Final Console ground truth reports 1 error/8 warnings retained after testing. Inspection attributes the error to a failed Pipeline `find_assets --type Prefab` command from this session, not project compilation/runtime; warnings are expected UseInfo overflow and Room hatching test emissions. Console was not cleared.
- Human feedback identified that `[ContextMenu]` commands were hidden in the component menu rather than visible in the normal Inspector. Added the Editor-only `ResidentDemandControllerEditor` with Play Mode-gated Add Resident, Refresh Demand Availability, and Inspect Demand State buttons that invoke the existing guarded production methods.
- Verified the custom Inspector is selected by Unity for the production controller. C# build and Unity recompile passed; focused Resident Demand EditMode passed 22/22, full EditMode passed 46/46, and `git diff --check` passed with existing line-ending notices only.
- Human verification found production `Available Usage Pool=None` despite a TV. Live inspection showed the active TV at `House/Products/TV` has UsageType Fun but no parent `RoomArea`; the gameplay Room is a sibling whose collider contains the TV. Parent-only discovery therefore excluded the TV, produced zero usage snapshots, and never called UseInfo binding/hiding, leaving authored sample icons visible.
- Corrected Product eligibility to preserve parent-room support while also accepting spatial containment by an active gameplay-enabled RoomArea collider. Added an EditMode regression for a Product without a RoomArea parent but inside the gameplay room. Runtime reinspection now reports pool Fun, Resident 01 WAITING/Fun, one ProductUsageSnapshot, and inactive UseInfo for the disconnected/unpowered TV. Focused tests pass 23/23 and full EditMode passes 47/47.
- Phase D added explicit Required EXP authoring/import rows and `SessionProgressConfig.asset` (initial Satisfaction 100; temporary Room 1=20 through Room 15=160), authoritative `SessionProgressController`, one-shot progression signals, explicit future threshold acknowledgement, and event-driven GameStatusInfo HUD presentation.
- Matching Editor wired the production controller/config and `UI_GameInfo.prefab` coordinator/nested status view without hierarchy or visual redesign. Runtime Play Mode inspection reports initialized=true, Satisfaction=100, EXP=0/20, RoomCount=1, depleted=false, and slider values 1/0; no new warnings/errors appeared during the inspected interval.
- Phase D verification passes: core Release 39/39, focused progression EditMode 3/3, full EditMode 50/50, Unity recompile, `verify-unity.ps1`, `verify-fast.ps1`, and `git diff --check`. Task context remains WARN within hard limits. `verify-harness.ps1` fails only on its generic intentional Unity-content guard; no bypass was attempted. A read-only review agent failed with HTTP 429, so no review output is claimed.
- Human Verification PASS recorded for the complete player-facing Resident Demand, assignment, Satisfaction/Patience, UI, Global Satisfaction/EXP, HUD, and 30% color-boundary flow.
- Final official progression checks passed: `SessionProgressStateTests` 9/9 and `SessionProgressIntegrationTests` 3/3, covering one-shot depletion/threshold behavior and RoomCount-specific RequiredEXP updates.
- Phase D and TASK-010 marked complete; task context compacted for finalization. GameFlow remains a separate TASK-011 boundary.
