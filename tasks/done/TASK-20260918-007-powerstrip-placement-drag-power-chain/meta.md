# Task Metadata

- **ID:** TASK-20260918-007
- **Title:** PowerStrip Placement + Drag + Power Chain
- **Status:** done
- **Owner:** MoHoDu
- **Agent:** Claude Code
- **Domain:** Connection / Power
- **Base:** dev
- **Branch:** task/TASK-20260918-007-ui-integration (isolated task worktree)
- **Started:** 2026-09-18
- **Completed:** 2026-09-20
- **Updated:** 2026-09-20 (Human Verification PASS 7/7; implementation committed and pushed by user)
- **Current Stage:** DONE. Runtime implementation, unified variable-count Wall Outlet integration, automated verification, scene audit, and all seven Human Verification checks are complete. Reward System, persistence, production Room Generation integration, gameplay upgrade UI, and socket-count decrease remain intentionally out of scope.
- **Current Skill:** task completion

## Context

TASK-20260918-006 closed DONE (8-direction Cable Routing + Grid foundation). This Task builds PowerStrip (Multitap) gameplay on top of it: Head placement/drag, PowerStrip's own upstream Plug connection, and the full Wall Outlet → PowerStrip → Product power chain (Powered/Flow/budget validation).

## Human Setup Check (done before implementation, per explicit instruction)

Checked live scene state (Play mode + `eval`) before writing any code:

- **PowerStrip instances already exist** — no Human Setup Required blocker:
  - `Multitap_One` at world (2.09, 2.06) — Walkable cell, 1 Socket.
  - `Multitap_Two` at world (-1.33, -1.19) — Walkable cell, 2 Sockets.
  - Both on Walkable grid cells (not wall-overlapping), both currently unconnected (`Plug.ConnectedSocket == null`), both structurally identical to Product's Cable/Plug wiring (`Cable` child has `CableInfo`/`CableRoutingController`/`CablePowerFlowEffect`, `Plug` has `PlugConnector`/`PlugDragInput`/Collider2D).
  - **Correction, discovered later via `git diff` after this Task's own `save_scene` calls:** these two instances existed only in the running Editor's unsaved in-memory scene state — `git show HEAD:.../InfiniteMode.unity` had zero references to either Multitap prefab before this Task. They are now persisted to disk for the first time; nothing else in the scene changed (diff reviewed line-by-line: only the two placements' position/rotation/name overrides).
  - One `Wall_Outlet_One` exists in the scene at world (4, 0), 1 Socket, unconnected — usable as the upstream connection target for verification.
  - `HousePowerBudget.AllowedPowerWatts = 10` (already set, from a prior task).
- **Found two real wiring/data gaps in the existing `PowerStrip`/Multitap prefabs (not a Human Setup gap — a pre-existing incomplete wiring bug, fixed as part of this Task's implementation, not a design decision):**
  1. `PowerStrip.sockets` (`[SerializeField] private List<SocketConnector> sockets`) is correctly *sized* to match each prefab's actual `Socket`/`Socket (N)` child count, but **every list entry is `null`** — confirmed on all 5 `Multitap_{One..Five}.prefab`. This silently breaks `PowerValidationService.GetStripUsage`/`TryValidate`'s per-strip socket matching and would break this Task's cascade logic. Fixed by wiring each list slot to its already-present sibling `SocketConnector` component — a reference fix, not a new field or a visual change.
  2. `PowerStrip.allowedPowerWatts = 0` on all 5 prefabs (same "unset placeholder" class of issue `HousePowerBudget` had before a prior task set it to 10). A `0` budget cannot be demonstrated (every connection attempt would exceed it), so a non-zero **demo placeholder** value is required for this Task's own Automated/Human Verification to be meaningful. Set uniformly to `10` (matching `HousePowerBudget`'s own placeholder magnitude) on all 5 prefabs — explicitly a placeholder, not a balance decision (see Human Decisions).

## Allowed Scope

- Reuse the existing Grid (`CableRoutingGrid`/`CableRoutingGridService`), Cable/Plug/Socket connection system (`CableRoutingController`, `PlugConnector`, `SocketConnector`, `PlugSocketConnection`), and Power Validation (`PowerValidationService`, `HousePowerBudget`, `PowerStrip`) exactly as TASK-006/005 left them — extend, do not redesign.
- `Octoplug.Power.PowerStrip`: add `IsPowered` (get) + `SetPowered(bool)`, mirroring `ApplianceSource`'s existing pattern.
- New small shared surface only if it removes real duplication between `ApplianceSource`/`PowerStrip` powered-state handling in the cascade path (evaluate during implementation; keep minimal).
- `Octoplug.Power.Connection.PowerValidationService`: add `IsSocketSourceLive(SocketConnector socket)` — true if the socket's parent chain has no `PowerStrip` (i.e. it is Wall-Outlet-backed, always a live source) or its parent `PowerStrip.IsPowered` is true. Existing `TryValidate`/`GetHouseUsage`/`GetStripUsage` wattage logic unchanged.
- `Octoplug.Power.Cable.CableRoutingController`:
  - Resolve a `PowerStrip` parent (in addition to the existing `ApplianceSource` parent) — whichever exists, never both, no prefab-name branching.
  - After any connect/disconnect of Cable's own Plug, apply the resulting Powered/Flow state to whichever owner exists (Product or PowerStrip), and — new — recompute the FINAL powered value as "wattage/route validation passed" AND `IsSocketSourceLive(socket)"; a Product/PowerStrip's Plug can physically connect to an unpowered upstream Socket (matches "연결" and "Powered" being separate per the request), just stays not-Powered until the upstream chain goes live.
  - New public `RefreshPoweredFromUpstream()` — re-evaluates and applies this Cable's own Powered/Flow purely from its current connection (no new route/length/wattage checks), for reactive cascading.
  - New: when a `PowerStrip`'s own Powered state actually changes, cascade `RefreshPoweredFromUpstream()` to every Product/nested-strip currently connected to any of that strip's Sockets (via `SocketConnector.ConnectedPlug` → `GetComponentInParent<CableRoutingController>()`).
  - New public `RecomputePathFromCurrentOrigin()` — re-renders the Base/Flow path between Origin and the Plug's current position without touching Cable Length/route validity rules; used when the strip's Head (and therefore its Cable's Origin) moves.
- New `Octoplug.Power.Input.HeadDragInput` (mirrors `PlugDragInput`'s mouse-drag detection idiom; Plug- and Head-drag stay distinct capabilities per the request — Wall Outlet/Product never get this component).
- New `Octoplug.Power.Cable.PowerStripHeadController` (or equivalent name settled during implementation) on the Multitap root: drives the ROOT transform (not just the `Head` child) from `HeadDragInput` events — moving the root moves `Head`/`Sockets`/`Cable`'s `CableOrigin`/`PowerInfo` together as one rigid unit (they are already siblings under the root; no prefab reparenting needed). Grid-validates each candidate position via `CableRoutingGrid.IsWalkable`/`IsFreeForPlacement`, marks/unmarks its own occupied cell via the existing (previously-unused) `SetObjectOccupied` API, and calls the strip's own Cable's `RecomputePathFromCurrentOrigin()` each moved frame.
- Prefab component/reference additions only (no visual redesign): add a `Collider2D` + `HeadDragInput` to each `Multitap_{One..Five}.prefab`'s `Head` child, and the new head-controller component to each prefab's root — same pattern as TASK-001's original Collider2D rollout.
- Fix the two wiring gaps above (`PowerStrip.sockets` null entries, `allowedPowerWatts = 0`) on all 5 `Multitap_{One..Five}.prefab`.
- Bind the existing House HUD to `PowerValidationService.GetHouseUsage()` and `HousePowerBudget.AllowedPowerWatts`. Runtime `Image.color` and active-state changes are explicitly allowed: first Usage active icons use the authored Orange reference, the remaining active icons use authored Grey, and icons beyond Allowed are inactive. Preserve sprites, hierarchy, RectTransforms, layout, sorting, and authored design.
- Reuse the placed `UI_AlertText` instance for the four typed `ConnectionFailureReason` values, with one centralized enum-to-approved-copy mapping and Inspector-configurable auto-hide. Repeated alerts reuse the same object and restart the timer.
- House HUD refresh is event-driven: successful `PlugSocketConnection` mutations, House/PowerStrip allowance changes, and typed rejection presentation queue one coalesced authoritative meter refresh. UI remains presentation-only; validation/connection/cascade rules remain authoritative elsewhere.
- Keep `GameStatusInfo`, `UI_RoomInfo`, and `UI_SettingBtn` structurally and behaviorally untouched.

## Do Not Modify

- Any authored Prefab visual value: Sprite, Layout, Scale, Hierarchy, visual Position, or Sorting Order — for Product, Wall Outlet, Multitap, ProductInfo/Tooltip UI, Room. Runtime `Image.color` changes are allowed only for the existing House/PowerStrip power-meter binding. No reparenting of `Head`/`Sockets`/`Cable`/`PowerInfo` under each other.
- 8-direction A*, corner-cutting, Door-only crossing, Grid cellSize, Cable Length accounting/truncation rules for Plug drag — unchanged.
- `PlugConnector`/`SocketConnector`/`PlugSocketConnection` pairing semantics for a Plug's own drag/connect (only the *Powered-value computation* changes, not the pairing mechanism).
- `ApplianceSource`'s own fields/behavior beyond what `CableRoutingController` already calls (`SetPowered`) — no redesign of Product-side Power Validation.
- Socket count/upgrade tiers, Cable Length balance values, resident demand, satisfaction, leveling, rewards, procedural Room generation, top-alert UI final design, Product-overlap alpha fade, sound — explicitly out of scope per the request.
- House HUD binding is no longer Human Setup Required: runtime use of the authored Orange/Grey colors is approved. Do not change its sprites, hierarchy, transforms, layout, sorting, or prefab design.
- PowerStrip `PowerInfo` remains at 5 authored icons. That pool now represents every finalized initial allowance (`3/3/4/5/5`) correctly, but cannot represent future upgraded allowances `6–10`. Do not clamp or under-report Validation load, clone icons, couple icon count to Allowed Power, or expand its Prefab design in this balance-only pass.
- **Power UI binding pass (2026-09-19):** Interaction/Head Drag/Socket UX/Cable routing are explicitly out of scope this pass and were not touched. Power graph code (`PowerValidationService.cs`) is likewise out of scope for this pass, with exactly one user-approved exception recorded under Human Decisions below (a one-line null-collision fix in `CollectDownstreamProducts`) — no other Power-graph behavior changed.

## AI Setup Allowed

- Existing Scene UI instances may receive data/event bindings only: add one non-visual coordinator component to the existing `ConnectionDirector` and serialize references to the already-placed `HousePowerInfo` and `UI_AlertText` objects. No UI objects may be created, reparented, resized, restyled, or repositioned.
- The 2026-09-20 runtime-control pass may add a removable custom Inspector under `Assets/Editor` that is disabled outside Play Mode and invokes only public production +1 APIs. It may not use `SerializedObject`, reflection/private-field writes, direct visual toggles, prefab replacement, `EditorUtility.SetDirty`, Undo as state storage, or scene/prefab saving.
- No new scene-level PowerStrip/Wall Outlet/UI instance placement. No production gameplay upgrade UI.

## Exclusive Assets

- `Assets/01_Scripts/Power/PowerStrip.cs`
- `Assets/01_Scripts/Power/Connection/PowerValidationService.cs`
- `Assets/01_Scripts/Power/Cable/CableRoutingController.cs`
- `Assets/01_Scripts/Power/Input/HeadDragInput.cs`
- `Assets/01_Scripts/Power/Input/PlugDragInput.cs`
- New: `Assets/01_Scripts/Power/Input/PointerInteractionResolver.cs`
- `Assets/01_Scripts/Power/UI/ProductTooltipController.cs`
- `Assets/01_Scripts/Power/SocketConnector.cs`
- New: `Assets/01_Scripts/Power/Connection/ConnectionFailureReason.cs`
- `Assets/01_Scripts/Power/Cable/PowerStripHeadController.cs`
- `Assets/03_Prefabs/Multitaps/Multitap_{One,Two,Three,Four,Five}.prefab` (legacy sources remain in place; no deletion in the ActiveSocketCount pass)
- `Assets/03_Prefabs/Multitaps/Multitap.prefab` (new single PowerStrip Source of Truth; component/reference/state wiring only)
- `Assets/03_Prefabs/Multitaps/Head.prefab` (existing Whole01–05/End/BoxCollider2D reference and initial-active-state wiring only)
- `Assets/03_Prefabs/Multitaps/Sockets.prefab` (existing Socket01–05 reference and initial-active-state wiring only)
- `Assets/00_Scenes/Demo/InfiniteMode.unity` (existing `ConnectionDirector` component/reference binding only; no UI object placement or visual property changes)
- New: `Assets/01_Scripts/Power/UI/PowerUiCoordinator.cs` and `.meta`
- UI prefab sources are inspection-only and must remain serialized-diff clean: `Assets/03_Prefabs/UI/{UI_GameInfo,HousePowerInfo,GameStatusInfo,UI_AlertText,UI_RoomInfo,UI_SettingBtn}.prefab`
- `Assets/03_Prefabs/Multitaps/PowerInfo.prefab` — component/reference wiring only this pass (added `PowerStripPowerInfoBinding`, wired its 5 icon references + 2 color-source references; icon pool remains 5 and supports the finalized initial allowances, while future allowances `6–10` require a separate expansion — no sprite/hierarchy/layout/color-asset change).
- New: `Assets/01_Scripts/Power/UI/PowerMeterPresenter.cs` and `.meta` (shared icon-state presenter used by both House and PowerStrip meters).
- New: `Assets/01_Scripts/Power/UI/PowerStripPowerInfoBinding.cs` and `.meta`.
- Stabilization scripts: `Assets/01_Scripts/Power/Grid/CableRoutingGrid.cs`, `Assets/01_Scripts/Power/Cable/CablePathRenderer.cs`, `Assets/01_Scripts/Power/UI/ProductClickInput.cs`, and the minimal new placement-footprint/resolver-driver scripts and `.meta` files.
- `Assets/03_Prefabs/Products/Cable.prefab` (only evidence-proven LineRenderer coordinate/initial-point corrections; preserve Base order 5, Flow order 6, shared path, width, material, Flow texture animation, and styling; any mesh conversion is decision-gated).
- Product prefabs `Assets/03_Prefabs/Products/{TV,Fan,Heater,Induction,Air_Conditioner}.prefab` (nonvisual placement-footprint wiring only).
- Wall Outlet prefabs `Assets/03_Prefabs/Wall_Outlets/Wall_Outlet_{One,Two,Three,Four,Five}.prefab` (nonvisual Socket hit/terminal-approach metadata only).
- `Assets/02_Resources/Font/Pretendard-Medium SDF.asset` and existing `Pretendard-Medium 1.otf` (repair the existing dynamic source reference and glyph coverage only; no new font asset).
- Runtime upgrade control pass: `Assets/01_Scripts/Power/{PowerStrip.cs,CableInfo.cs,PowerStripSocketCountFailure.cs}`; new typed upgrade failure enums and `.meta`; `Assets/01_Scripts/Power/Cable/CableRoutingController.cs`; `Assets/01_Scripts/Power/UI/{ProductTooltipController.cs,PowerStripPowerInfoBinding.cs}`; new `Assets/Editor/Power/PowerStripEditor.cs` and `.meta`; focused Editor/Unity tests and `.meta` if added.
- Verification foundation: `Assets/Tests/Editor/**` and `.meta`; `scripts/{verify-task.ps1,verify-unity.ps1}`; `harness/mcp/README.md`; current TASK-007 `meta.md`, `plan.md`, `todo.md`, and `handoff.md`.
- Unified sources for verification/reference repair only: `Assets/03_Prefabs/Multitaps/{Multitap.prefab,PowerInfo.prefab}`. Preserve hierarchy, sprites, transforms, layout, scale, sorting, and authored colors. The five legacy Multitap prefab sources remain untouched.
- Unified Wall Outlet continuation: `Assets/01_Scripts/Power/{WallOutlet.cs,SocketConnector.cs,PowerStripSocketLayout.cs}` plus the narrow shared-layout replacement/new files and `.meta`; `Assets/01_Scripts/Power/Grid/PlacementFootprint.cs`; `Assets/01_Scripts/Power/Connection/PowerValidationService.cs`; new `WallOutletSocketCountFailure.cs` and `.meta`; focused `Assets/Tests/Editor/**`; `scripts/verify-task.ps1`; `harness/mcp/README.md`; `docs/domains/connection-power.md`; `Assets/03_Prefabs/Wall_Outlets/Wall_Outlet.prefab`; and `Assets/00_Scenes/Demo/InfiniteMode.unity` for the authorized one-for-one scene migration after automated PASS.
- Read-only migration inputs, never modify/delete: `Assets/03_Prefabs/Wall_Outlets/Wall_Outlet_{One,Two,Three,Four,Five}.prefab`.

## Human Decisions

- **Power UI / Alert integration (confirmed 2026-09-18):** reuse only the placed Source-of-Truth UI instances. House uses one capacity range with the existing authored two visual states: current usage occupies the authored used state, the rest of the allowed range uses the authored unused state, and capacity beyond Allowed is inactive. If those authored states cannot be mapped unambiguously, stop with Human Setup Required.
- **Alert copy (confirmed 2026-09-18):** `HousePowerExceeded` → `집의 허용 전력을 초과했습니다.`, `PowerStripPowerExceeded` → `멀티탭의 허용 전력을 초과했습니다.`, `SelfConnection` → `같은 멀티탭에는 연결할 수 없습니다.`, `CircularConnection` → `순환 연결은 허용되지 않습니다.`
- **PowerStrip PowerInfo (confirmed 2026-09-18):** do not bind the current 5-icon pool to a 10W allowance; wait for Human Setup rather than clamp or change icon units.
- **Nested PowerStrip → PowerStrip connection (confirmed 2026-09-18):** explicitly allowed with no fixed depth limit. Self-connection (`Plug Owner == Socket Owner`) and every directed graph cycle are explicitly forbidden and must be rejected before `Connect` with typed failure reasons. Full downstream branch usage counts toward every ancestor PowerStrip budget.
- **Final stabilization behavior (confirmed 2026-09-19):** pointer priority is `UI > Plug > Product > Socket > PowerStrip Head > Empty`, with one owner captured from Pointer Down through Pointer Up. A Head is movable only while its own Plug and every downstream Socket are disconnected; a connected-Head attempt is silent. Product/Product, Product/PowerStrip, and PowerStrip/PowerStrip placement overlap is forbidden through shared Grid placement occupancy while cable traversal remains independent.
- **Socket/graph/cable behavior (confirmed 2026-09-19):** magnetic acquisition uses raw pointer position, authored Socket size and Grid cell size; Wall Outlets are terminal room-side endpoints. Prospective physical downstream load is validated against target/ancestor strips and House before `Connect`, independent of current Powered state. Candidate detection runs before ordinary wall/Grid clamping every drag frame and may snap-preview without graph mutation. Cable repairs preserve Base order 5, Flow order 6, authored width/material, and the exact shared path.
- **Remaining UX/UI behavior (confirmed 2026-09-19):** Head hit padding is small, Inspector-adjustable, and Body-bounds-derived while higher-priority targets still win. PowerStrip drag is free preview with the existing reservation retained; full-footprint validation and nearest-valid Grid fallback occur only on Pointer Up, otherwise the exact drag-start position is restored. House power UI uses authoritative graph values and runtime authored Orange/Grey colors. PowerStrip UI must report the 5-icon/10W shortage without clamping. First-visible-frame Cable state and LineRenderer geometry require evidence-led diagnosis; stop for a Human Decision before any mesh conversion.

- **Mini Metro interaction model (confirmed 2026-09-19, final):** Head acquisition is generous (0.5 world units) and Inspector-adjustable, still losing every overlap to Plug/Product/Socket. PowerStrip drag uses a grab offset (the clicked point stays under the pointer) and is continuously legal every rendered frame via `PlacementFootprint.CanReserveAt` with half-cell substeps and axis-separated (X/Y/both) sliding — Room/Wall/Product/PowerStrip legality is never violated even transiently, so Pointer Up never searches for a nearby cell or restores an earlier position; it simply commits the already-legal current position. Socket acquisition is floored at `socketDetachRadius` so first-touch acquisition is never stricter than reconnect hysteresis. All Plug/Socket graph, Powered, and Flow mutation is deferred to Pointer Up (transactional drag) — mid-drag only visual preview and a sticky "drifted past detach radius" flag change. The cable's route-topology anchor (interior A* target cell) is cached and only recomputed on an actual Grid-cell change; the terminal cell-center point is never rendered for a loose drag, eliminating the per-pixel endpoint-absorption/elbow-flip toggle. LineRenderer corner-vertex smoothing (`numCornerVertices`), silently dropped during the prior Stabilization pass, is restored.
- **Footprint fidelity (confirmed 2026-09-19, final):** Multitap `PlacementFootprint`/Head-hit reliance on `Head/Body`'s full (transparent-margin-inclusive) sprite bounds is replaced by an added, non-visual, trigger `BoxCollider2D` on each Multitap root sized to that prefab's alpha-trimmed opaque Body extent (computed offline from the source PNGs; ratios ~0.60–0.92 of the full renderer bounds per axis, per prefab). No sprite/scale/hierarchy/visual change. Product prefabs were audited (Icon collider vs. Icon+Background visual union, within ~9% in every case) and left unchanged — no evidence of a placement-relevant oversize/undersize.
- **House UI color-aliasing (found during exploration, fixed 2026-09-19):** `PowerUiCoordinator.houseUsedColorSource`/`houseUnusedColorSource` were serialized to the same `Image` objects as `housePowerIcons[0]`/`[1]`; reading their live `.color` every refresh meant the loop overwrote its own "authored" source on the first refresh, permanently losing Orange thereafter. Fixed by caching both colors once (before the first refresh) rather than re-reading the (now-mutated) icons — no new serialized reference, no visual change.

- **Head/Plug drag unification (confirmed 2026-09-19):** Human Verification passed Wall Socket magnetic UX, cable flip/spike, and Self/Circular/Chain — none of those, nor Power UI/Cable Grid/Cable renderer/Power graph, may be touched this pass. Remaining FAILs (Head hard to grab, "once missed, stays missed", Head jankier than Plug, sudden jump/capture-release, no user-adjustable click areas) require making Head drag mechanically identical to Plug drag: an explicit, Inspector-sized Collider2D is the sole hit-test source (no Renderer-bounds/padding computation); the interaction target is decided once at Pointer Down and held unconditionally to Pointer Up (no per-frame eligibility re-check, no per-frame re-hit-test); once captured, the root follows the pointer directly (grab offset only) with just a cheap O(1) Room-bounds clamp, deferring all Product/PowerStrip-overlap/full-footprint validation to Pointer Up (commit in place, nearest-valid snap, or exact drag-start restore). A loose Plug authored outside the walkable Room is corrected at runtime (Plug transform only, never the authored Prefab or the Product/PowerStrip root) to the nearest valid in-Room position. User accepted this round's report ("완벽해") and scoped the next pass to Power UI binding only.

- **Power UI binding scope (confirmed 2026-09-19, latest):** fix only the Power UI binding — Interaction/Head Drag/Socket UX/Cable routing untouched. Verify wiring is a genuine runtime binding (a real component on the real Scene/Prefab object, not just code that exists). House meter: attach a minimal binding to `HousePowerInfo`, sourcing Usage from `PowerValidationService.GetHouseUsage()` and Allowed from `HousePowerBudget.AllowedPowerWatts`; display rule is exactly Allowed icons active, `index < Usage` Orange, remaining active icons Grey, beyond Allowed inactive. PowerStrip meter: attach the same binding structure to each Multitap's `PowerInfo`, sourcing Usage from the strip's recursive downstream load and Allowed from `PowerStrip.AllowedPowerWatts`; since Allowed=10 but the icon pool is 5, exact display is currently impossible — wire the binding now anyway and report the shortage as Human Setup Required, structured so the binding works as-is once the pool is resized to 10, with no further code change. Scene/Prefab Source of Truth: hierarchy/layout/position/sprite/authored-color/scale changes forbidden; binding-component addition, reference wiring, active-state changes, and runtime Orange/Grey changes allowed. Runtime Verification must observe the real Scene object's component actually executing and the real `Image` state actually changing — calling the controller via reflection alone does not count as PASS. Human Verification is limited to 3 questions (House icon updates immediately on connect/disconnect; Orange grows from the left with multiple Products; reverts to Grey on disconnect); PowerStrip UI is separately Human Setup Required until the icon pool is resized.
- **Power-graph exception, user-approved (2026-09-19):** while verifying the Power UI binding end-to-end, found a pre-existing bug in `PowerValidationService.CollectDownstreamProducts` — its `edgeIsBeingReplaced` check compared `product == proposedProduct` / `childStrip == proposedStrip` unconditionally, so a plain query (`proposedProduct`/`proposedStrip` both `null`) degenerated to `null == null` (always true) and silently excluded every genuinely-connected downstream product/nested strip, making `GetHouseUsage()`/`GetStripUsage()` always return 0 for anything connected through a PowerStrip rather than directly to a Wall Outlet. Asked the user whether to apply a minimal one-line fix now or defer as a separate bug report; **user selected the minimal one-line fix ("한 줄만 최소 수정 (권장)")**. Applied: each half of `edgeIsBeingReplaced` is now guarded by its own proposed reference actually being non-null, so `TryValidate`/`TryValidateGraphConnection` (which always pass a real proposed reference) are unaffected, and plain queries now correctly include every real downstream edge. This is the only Power-graph change made this pass.

- **Finalized power-balance matrix (confirmed 2026-09-19):** House initial/current allowance = `8`, House maximum = `22`. PowerStrip initial/current allowances are independent per prefab: `Multitap_One=3`, `Multitap_Two=3`, `Multitap_Three=4`, `Multitap_Four=5`, `Multitap_Five=5`; each strip's maximum is independently `10`. Socket count and Allowed Power are separate authoritative stats. A future reward may increase socket count, Allowed Power, or both, but no Reward System or upgrade mutation was implemented in this pass. House UI capacity remains 22. The current shared PowerStrip UI pool remains 5; it truthfully covers all finalized initial values but requires a separate future asset/setup pass to display upgraded values 6–10.

## Open Decisions

- None for current/maximum power-balance values. Future Reward System mechanics and the PowerStrip UI expansion from 5 to 10 icons remain separate scoped work.
- **ActiveSocketCount expansion placement failure:** runtime will fail atomically in place with no state change. Player-facing feedback and whether a future action may search/move to a nearby legal position require a Human Decision.
- **Socket-count decreases:** current Reward design is increase-only. Every decrease is explicitly rejected with `SocketCountDecreaseUnsupported`, without disconnecting or partially updating. Any future decrease workflow requires a separately scoped Human Decision.
- **PowerInfo positions for counts 1–5:** finalized and authored in `Multitap.prefab` as `(-0.23, 0.5)`, `(-0.46, 0.5)`, `(-0.69, 0.5)`, `(-0.92, 0.5)`, and `(-1.15, 0.5)`. This is no longer `HUMAN_SETUP_REQUIRED`; code must continue to consume these authored values without bounds-based inference.

## Verification State

- Human verification result (2026-09-18): PASS — PowerStrip Head Drag, Wall passage prevention, Wall Outlet → PowerStrip → Product chain, and Power Flow. Follow-up blocker: overlapping Plug/Head interaction made pointer intent ambiguous.
- Final interaction scope: deterministic `UI > Plug > Product > Socket > eligible Head > Empty` pointer-down capture through pointer-up; Body-sized Head interaction; fully-disconnected-only Head movement; arbitrary-depth acyclic PowerStrip chains; pre-Connect Self/Cycle checks; recursive physical-load validation and power propagation; typed failure reasons/hook.
- Automated verification (2026-09-18): full Wall Outlet → PowerStrip → Product power chain (both directions, connect/disconnect cascade), PowerStrip/House budget rejection, Head Drag (valid move, Wall-blocked, outside-room-blocked, other-strip-occupied-blocked), Head-drag-time path re-routing while connected, existing Product→Wall-Outlet-direct regression — all PASS, zero Console errors. Two real bugs found and fixed during this pass (before any Human Verification) — see `handoff.md`/`log.md` for detail: (1) `RecomputePathFromCurrentOrigin` couldn't route to a connected Socket (Approach-Point logic missing), (2) Head drag silently dragged the connected Plug along via ordinary transform parenting.
- Stabilization verification (2026-09-19): C# build PASS (0 warnings/errors); direct Unity recompile PASS; Console Error 0; prefab footprint/terminal/font wiring PASS; all four complete Korean messages pass TMP `HasCharacters`; `verify-fast` PASS; `git diff --check` clean apart from line-ending notices; EditMode test count is zero (`NO_PROJECT_TESTS`).
- Final stabilization verification (2026-09-19): Head padding is Body-renderer-derived (`0.08` world-unit padding); placement preview is free with retained ownership and release-time full-footprint commit/nearest-valid/exact-start fallback; raw-pointer Socket preview runs before clamp; House HUD is bound to authoritative usage/allowance using the existing Orange/Grey colors; startup Grid ordering and stale cable geometry are deterministic; endpoint-adjacent sub-width/reversing cable segments are removed without changing A*. Base/Flow shared-path equality and sorting 5/6 passed the runtime probe. `Multitap_One` has an authored Plug outside the walkable Grid, so both initial LineRenderers are intentionally cleared instead of showing stale detached geometry.
- Verification: HUMAN_VERIFY_REQUIRED. Logic/runtime integration and automated checks pass, but real pointer feel, placement outcomes/reservation behavior, Wall Socket side acquisition, and final Game-view cable appearance still require Human Verification. House meter Human Verification passed 3/3. PowerStrip `PowerInfo` now represents every finalized initial allowance with its existing 5 icons; only future allowances `6–10` remain deferred pending a separate UI-capacity expansion. Task remains active; no Git wrap-up or next feature.
- **Mini Metro stabilization verification (2026-09-19, final):** `dotnet build`/Unity `recompile` PASS (0 warnings/errors); a 9-check Play-mode logic/runtime-integration harness (grab offset, open-space move, axis-separated sliding, no-teleport release, connected-Head immobility, Socket acquisition radius floor, corner-vertex restoration, House color-fix stability across refreshes, transactional-drag deferred disconnect/restore, stable-anchor route-cache reuse within one Grid cell) — all 9 PASS on a fresh Play session; Console ground-truth 0 errors (2 pre-existing/expected warnings: the documented `Multitap_One` out-of-grid authored Plug, and the documented non-deterministic first-frame `CableRoutingGridService` race). `verify-fast.ps1` PASS; `git diff --check` clean (line-ending notices only). Real pointer feel, placement/collision feel, and final Game-view cable appearance remain Human Verification.
- **Head/Plug drag unification verification (2026-09-19):** `dotnet build`/Unity `recompile` PASS. A 7-check Play-mode logic/runtime-integration harness — collider-only hit source confirmed (Renderer-bounds/padding fields verifiably removed), direct pointer-follow never blocked mid-drag by Product/PowerStrip occupancy, cheap Room-bounds clamp holds both extremes, an invalid release drop is never committed and the actual landing cell is grid-valid, a valid release drop commits near the requested point, no per-frame eligibility re-derivation, and `Multitap_One`'s previously-out-of-grid authored Plug is now walkable after the new runtime correction — all 7 PASS on a fresh Play session. A quick regression sweep (Self-connection rejection, Cable corner-vertex/sorting sanity) also PASS. Found and fixed one real bug during this pass: `Physics2D.autoSyncTransforms` is disabled project-wide (`ProjectSettings/Physics2DSettings.asset`), so `PlacementFootprint`'s Collider2D bounds could read stale (pre-move) data immediately after a same-frame `transform.position` drag update; `Physics2D.SyncTransforms()` is now called once before `PowerStripHeadController`'s release-time validation. Console ground-truth 0 errors, 1 expected diagnostic warning (the new initial-Plug-correction notice for `Multitap_One`). `verify-fast.ps1` PASS; `git diff --check` clean.
- **Power UI binding verification (2026-09-19, latest):** confirmed real scene wiring: `PowerUiCoordinator` genuinely exists on `/Managers/ConnectionDirector` referencing 22 real `Image` objects at `/Canvas/HUD_Area/UI_GameInfo/HousePowerInfo/Power`. Built a shared `PowerMeterPresenter` (icon-state logic used by both meters) and a new `PowerStripPowerInfoBinding`, wired onto the shared `PowerInfo.prefab` (propagates to all 5 Multitaps). Found and, with explicit user approval, fixed a real pre-existing Power-graph bug (see Human Decisions) that made nested-chain usage always compute as 0. Genuine end-to-end runtime verification (real `PlugSocketConnection.Connect`/`Disconnect` public-API calls only, external readback of the real `Image.color`/`activeSelf` — `PowerUiCoordinator`'s own methods never invoked directly): House meter direct connect/disconnect PASS (Grey→Orange on connect, Orange→Grey on disconnect, first-icon observed); nested Wall→Strip→Product chain PASS (`GetHouseUsage()` 0→3→0, icon followed the same transition); PowerStrip meter's Human-Setup-Required error confirmed firing via a real event-driven refresh (Console: `"...Usage=3 and Allowed=10...Multitap_Two..."`), not a reflection call. The "1W→first icon Orange" numeric example from the request could not be exercised literally — only TV (3W) is currently placed in the demo scene as an `ApplianceSource`; no other product was added or scene-placed to manufacture a 1W case (out of scope: no Scene/Prefab placement changes). Scene confirmed restored to fully-disconnected baseline (`HouseUsage=0`) after all tests. `dotnet build` PASS (0 warnings/errors); Unity `recompile` `up_to_date`; `verify-fast.ps1` PASS (only the pre-existing context-budget line-count warnings); `git diff --check`'s sole flag is Unity's own universal empty `m_Name: ` field on the newly added `PowerStripPowerInfoBinding` component in `PowerInfo.prefab` — present identically on every other untouched component in the same file and in unrelated prefabs, not a real whitespace-hygiene defect, and not hand-edited per the no-hand-edit-Unity-YAML policy. `AgentScripts/` cleaned up. Unity project tests: zero discovered (`NO_PROJECT_TESTS`).
- **Environment note (2026-09-19):** this session's Play-mode frame loop was found stuck at `FrameCount: 1` despite real time advancing, which stalls any `StartCoroutine`-based refresh (both meters use one) — `set_autotick` alone did not resolve it, but exiting and re-entering Play mode after autotick was confirmed active did (frame count then advanced normally, e.g. ~6500 frames in ~27s). Recorded here since it blocked genuine coroutine-completion verification until resolved, and a future session hitting a frozen `FrameCount` should re-enter Play mode rather than assume the binding logic is broken. Note this freeze is intermittent/environment-specific to this headless CLI session — it did not affect the user's own real interactive Play session.
- **PowerStrip log-spam follow-up (2026-09-19):** House Power UI Human Verification PASS on all 3 questions. User reported the PowerStrip meter "에러가 생겨서 아무것도 안돼" — investigated live and confirmed the wiring/logic is correct (no exception, correct references) and the "nothing displays" part is the already-reported icon-shortage limitation working as designed. Found and fixed a real issue: the Human Setup Required error re-logged on every graph change anywhere in the house rather than once per distinct shortage, flooding the console on ordinary connect/disconnect activity. Fixed in `PowerStripPowerInfoBinding.cs` (dedupe on `(icon pool size, AllowedPowerWatts)`); verified via a live 3-cycle connect/disconnect test that console error count no longer increases. The underlying icon-shortage limitation itself is unchanged and still Human Setup Required.
